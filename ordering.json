{
 "_": "Precomputed blocks from the three versions in samples/, extracted with the same code that produced every other number here. Shipped as data so this repo needs no parser. v0 is before any review, v1 after the comments were cut, v2 after the renames. `comparisons` are what `python ordering.py` runs with no arguments.",
 "versions": {
  "v0": {
   "comment_blocks": [
    {
     "line": 8,
     "state": {
      "comment": "// Compiles authored compositions into runtime archetypes. Declare (before world build, templates\n// are builder-only) walks scene initializers and the collection, then follows every definition\n// reference transitively into one TemplateRegistry — reachability is what makes a definition\n// declared, so the collection only needs the roots nothing references (spawn-by-key from code or a\n// save). Every composition funnels through RegisterEntry, which recurses into the\n// two reference-carrying modules — AbilityOwner (each ability's definition) and\n// ActionTimeline (each node's def) — minting a generated EntityDefKey for every inline definition\n// and stamping it back onto the config so Bake resolves references by key. BuildBlobRegistry (after\n// build) bakes every keyed definition (asset + generated) into CDefinitionRegistry; each\n// ActionTimeline module bakes its TimelineBlob as a side-effect. Scene and runtime spawn share one\n// path: EntityInitializer bakes a transient per-instance blob and spawns it through\n// EntitySpawnUtility, same as spawn-by-key (see notes/entity-initializer-design.md).",
      "annotated_code": "\tpublic sealed class DefinitionCompiler\n\t{\n\t\treadonly struct Entry\n\t\t{\n\t\t\tpublic readonly string Name;\n\t\t\tpublic readonly IReadOnlyList<ModuleConfig> Modules;\n\t\t\tpublic readonly IReadOnlyList<NestedDefinition> Nested;\n\t\t\tpublic readonly TemplateRegistry.RegisteredArchetype Archetype;\n\n\t\t\tpublic Entry(string name, IReadOnlyList<ModuleConfig> modules, IReadOnlyList<NestedDefinition> nested, TemplateRegistry.RegisteredArchetype archetype)\n\t\t\t{\n\t\t\t\tName = name;\n\t\t\t\tModules = modules;\n\t\t\t\tNested = nested;\n\t\t\t\tArchetype = archetype;\n\t\t\t}\n\t\t}\n\n\t\tstatic readonly IReadOnlyList<NestedDefinition> NoNested = Array.Empty<NestedDefinition>();\n\n\t\treadonly TemplateRegistry _registry = new();\n\n\t\t// Every composition declared this run, keyed or not — scene compositions bake transient, so they\n\t\t// have no key but still carry a nested list to validate.\n\t\treadonly List<Entry> _entries = new();\n\n\t\t// The keyed subset, by index into _entries: what BuildBlobRegistry publishes and what a nested\n\t\t// or spawn reference resolves through.\n\t\treadonly Dictionary<EntityDefKey, int> _byKey = new();\n\n\t\t// Timeline node config -> its generated key. Dedups a shared composition declared more than\n\t\t// once (e.g. one definition asset used by several scene initializers) to one node def. Per-run\n\t\t// (new compiler each world), so it never resolves to a stale key from a prior run.\n\t\treadonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();\n\n\t\t// Inline spawn definition -> its generated key. Same per-run lifetime as _nodeKeys.\n\t\treadonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();\n\n\t\t// Definition asset -> its key, for assets already declared. Also the re-entry guard: a reference\n\t\t// cycle resolves to the key instead of recursing.\n\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();\n\n\t\tint _generatedCounter;\n\t\tint _timelineCount;\n\n\t\t// Declare phase — before world construction. Scene compositions and every collection definition\n\t\t// register their archetypes (a definition gets a template whether or not a scene references it,\n\t\t// so any spawn-by-key has a group to resolve).\n\t\tinternal void Declare(Initializer[] sceneInitializers, EntityDefinitionCollection collection = null)\n\t\t{\n\t\t\tforeach (var initializer in sceneInitializers)\n\t\t\t\tif (initializer is EntityInitializer entityInitializer)\n\t\t\t\t\tentityInitializer.DeclareTemplate(this);\n\n\t\t\tDeclareDefinitions(collection);\n\t\t}\n\n\t\t// Feeds the registered archetypes to the WorldBuilder; call once after Declare.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n// … (truncated)"
     }
    },
    {
     "line": 42,
     "state": {
      "comment": "// Every composition declared this run, keyed or not — scene compositions bake transient, so they\n// have no key but still carry a nested list to validate.",
      "annotated_code": "\t\treadonly List<Entry> _entries = new();\n\n\t\t// The keyed subset, by index into _entries: what BuildBlobRegistry publishes and what a nested\n\t\t// or spawn reference resolves through.\n\t\treadonly Dictionary<EntityDefKey, int> _byKey = new();\n\n\t\t// Timeline node config -> its generated key. Dedups a shared composition declared more than\n\t\t// once (e.g. one definition asset used by several scene initializers) to one node def. Per-run\n\t\t// (new compiler each world), so it never resolves to a stale key from a prior run.\n\t\treadonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();\n\n\t\t// Inline spawn definition -> its generated key. Same per-run lifetime as _nodeKeys.\n\t\treadonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();\n\n\t\t// Definition asset -> its key, for assets already declared. Also the re-entry guard: a reference\n\t\t// cycle resolves to the key instead of recursing.\n\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();\n\n\t\tint _generatedCounter;\n\t\tint _timelineCount;\n\n\t\t// Declare phase — before world construction. Scene compositions and every collection definition\n\t\t// register their archetypes (a definition gets a template whether or not a scene references it,\n\t\t// so any spawn-by-key has a group to resolve).\n\t\tinternal void Declare(Initializer[] sceneInitializers, EntityDefinitionCollection collection = null)\n\t\t{\n\t\t\tforeach (var initializer in sceneInitializers)\n\t\t\t\tif (initializer is EntityInitializer entityInitializer)\n\t\t\t\t\tentityInitializer.DeclareTemplate(this);\n\n\t\t\tDeclareDefinitions(collection);\n\t\t}\n\n\t\t// Feeds the registered archetypes to the WorldBuilder; call once after Declare.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\t// Keyless entry point, for a scene composition that bakes transient.\n\t\tinternal TemplateRegistry.RegisteredArchetype RegisterComposition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = RegisterEntry(nameHint, configs, nested);\n\t\t\treturn _entries[entry].Archetype;\n\t\t}\n\n\t\t// Keyed entry point: same registration, plus the key -> entry lookup spawn resolves through.\n\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = RegisterEntry(nameHint, configs, nested);\n\n\t\t// Every composition funnels through here — one place that walks the modules, records the entry\n\t\t// and declares the nested defs, so no declare site can register a composition and forget half of\n\t\t// it. AbilityOwner and ActionTimeline carry references to other definitions — declare each\n\t\t// (minting a generated key for inline ones) and stamp the key back onto the config for Bake to\n\t\t// read. Augments configs in place, so the caller's list is the effective composition — the same\n\t\t// one that must reach Bake, or the template carries components the blob has no payload for.\n\t\t// Returns the entry index: the module walk and the nested declare both append entries of their\n\t\t// own, so neither end of the list identifies this one. The caller hands over configs — the entry\n\t\t// keeps it, so it must not be pooled or reused.\n\t\tint RegisterEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n// … (truncated)"
     }
    },
    {
     "line": 46,
     "state": {
      "comment": "// The keyed subset, by index into _entries: what BuildBlobRegistry publishes and what a nested\n// or spawn reference resolves through.",
      "annotated_code": "\t\treadonly Dictionary<EntityDefKey, int> _byKey = new();\n\n\t\t// Timeline node config -> its generated key. Dedups a shared composition declared more than\n\t\t// once (e.g. one definition asset used by several scene initializers) to one node def. Per-run\n\t\t// (new compiler each world), so it never resolves to a stale key from a prior run.\n\t\treadonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();\n\n\t\t// Inline spawn definition -> its generated key. Same per-run lifetime as _nodeKeys.\n\t\treadonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();\n\n\t\t// Definition asset -> its key, for assets already declared. Also the re-entry guard: a reference\n\t\t// cycle resolves to the key instead of recursing.\n\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();\n\n\t\tint _generatedCounter;\n\t\tint _timelineCount;\n\n\t\t// Declare phase — before world construction. Scene compositions and every collection definition\n\t\t// register their archetypes (a definition gets a template whether or not a scene references it,\n\t\t// so any spawn-by-key has a group to resolve).\n\t\tinternal void Declare(Initializer[] sceneInitializers, EntityDefinitionCollection collection = null)\n\t\t{\n\t\t\tforeach (var initializer in sceneInitializers)\n\t\t\t\tif (initializer is EntityInitializer entityInitializer)\n\t\t\t\t\tentityInitializer.DeclareTemplate(this);\n\n\t\t\tDeclareDefinitions(collection);\n\t\t}\n\n\t\t// Feeds the registered archetypes to the WorldBuilder; call once after Declare.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\t// Keyless entry point, for a scene composition that bakes transient.\n\t\tinternal TemplateRegistry.RegisteredArchetype RegisterComposition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = RegisterEntry(nameHint, configs, nested);\n\t\t\treturn _entries[entry].Archetype;\n\t\t}\n\n\t\t// Keyed entry point: same registration, plus the key -> entry lookup spawn resolves through.\n\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = RegisterEntry(nameHint, configs, nested);\n\n\t\t// Every composition funnels through here — one place that walks the modules, records the entry\n\t\t// and declares the nested defs, so no declare site can register a composition and forget half of\n\t\t// it. AbilityOwner and ActionTimeline carry references to other definitions — declare each\n\t\t// (minting a generated key for inline ones) and stamp the key back onto the config for Bake to\n\t\t// read. Augments configs in place, so the caller's list is the effective composition — the same\n\t\t// one that must reach Bake, or the template carries components the blob has no payload for.\n\t\t// Returns the entry index: the module walk and the nested declare both append entries of their\n\t\t// own, so neither end of the list identifies this one. The caller hands over configs — the entry\n\t\t// keeps it, so it must not be pooled or reused.\n\t\tint RegisterEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n// … (truncated)"
     }
    },
    {
     "line": 50,
     "state": {
      "comment": "// Timeline node config -> its generated key. Dedups a shared composition declared more than\n// once (e.g. one definition asset used by several scene initializers) to one node def. Per-run\n// (new compiler each world), so it never resolves to a stale key from a prior run.",
      "annotated_code": "\t\treadonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();\n\n\t\t// Inline spawn definition -> its generated key. Same per-run lifetime as _nodeKeys.\n\t\treadonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();\n\n\t\t// Definition asset -> its key, for assets already declared. Also the re-entry guard: a reference\n\t\t// cycle resolves to the key instead of recursing.\n\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();\n\n\t\tint _generatedCounter;\n\t\tint _timelineCount;\n\n\t\t// Declare phase — before world construction. Scene compositions and every collection definition\n\t\t// register their archetypes (a definition gets a template whether or not a scene references it,\n\t\t// so any spawn-by-key has a group to resolve).\n\t\tinternal void Declare(Initializer[] sceneInitializers, EntityDefinitionCollection collection = null)\n\t\t{\n\t\t\tforeach (var initializer in sceneInitializers)\n\t\t\t\tif (initializer is EntityInitializer entityInitializer)\n\t\t\t\t\tentityInitializer.DeclareTemplate(this);\n\n\t\t\tDeclareDefinitions(collection);\n\t\t}\n\n\t\t// Feeds the registered archetypes to the WorldBuilder; call once after Declare.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\t// Keyless entry point, for a scene composition that bakes transient.\n\t\tinternal TemplateRegistry.RegisteredArchetype RegisterComposition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = RegisterEntry(nameHint, configs, nested);\n\t\t\treturn _entries[entry].Archetype;\n\t\t}\n\n\t\t// Keyed entry point: same registration, plus the key -> entry lookup spawn resolves through.\n\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = RegisterEntry(nameHint, configs, nested);\n\n\t\t// Every composition funnels through here — one place that walks the modules, records the entry\n\t\t// and declares the nested defs, so no declare site can register a composition and forget half of\n\t\t// it. AbilityOwner and ActionTimeline carry references to other definitions — declare each\n\t\t// (minting a generated key for inline ones) and stamp the key back onto the config for Bake to\n\t\t// read. Augments configs in place, so the caller's list is the effective composition — the same\n\t\t// one that must reach Bake, or the template carries components the blob has no payload for.\n\t\t// Returns the entry index: the module walk and the nested declare both append entries of their\n\t\t// own, so neither end of the list identifies this one. The caller hands over configs — the entry\n\t\t// keeps it, so it must not be pooled or reused.\n\t\tint RegisterEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n// … (truncated)"
     }
    },
    {
     "line": 55,
     "state": {
      "comment": "// Inline spawn definition -> its generated key. Same per-run lifetime as _nodeKeys.",
      "annotated_code": "\t\treadonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();\n\n\t\t// Definition asset -> its key, for assets already declared. Also the re-entry guard: a reference\n\t\t// cycle resolves to the key instead of recursing.\n\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();\n\n\t\tint _generatedCounter;\n\t\tint _timelineCount;\n\n\t\t// Declare phase — before world construction. Scene compositions and every collection definition\n\t\t// register their archetypes (a definition gets a template whether or not a scene references it,\n\t\t// so any spawn-by-key has a group to resolve).\n\t\tinternal void Declare(Initializer[] sceneInitializers, EntityDefinitionCollection collection = null)\n\t\t{\n\t\t\tforeach (var initializer in sceneInitializers)\n\t\t\t\tif (initializer is EntityInitializer entityInitializer)\n\t\t\t\t\tentityInitializer.DeclareTemplate(this);\n\n\t\t\tDeclareDefinitions(collection);\n\t\t}\n\n\t\t// Feeds the registered archetypes to the WorldBuilder; call once after Declare.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\t// Keyless entry point, for a scene composition that bakes transient.\n\t\tinternal TemplateRegistry.RegisteredArchetype RegisterComposition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = RegisterEntry(nameHint, configs, nested);\n\t\t\treturn _entries[entry].Archetype;\n\t\t}\n\n\t\t// Keyed entry point: same registration, plus the key -> entry lookup spawn resolves through.\n\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = RegisterEntry(nameHint, configs, nested);\n\n\t\t// Every composition funnels through here — one place that walks the modules, records the entry\n\t\t// and declares the nested defs, so no declare site can register a composition and forget half of\n\t\t// it. AbilityOwner and ActionTimeline carry references to other definitions — declare each\n\t\t// (minting a generated key for inline ones) and stamp the key back onto the config for Bake to\n\t\t// read. Augments configs in place, so the caller's list is the effective composition — the same\n\t\t// one that must reach Bake, or the template carries components the blob has no payload for.\n\t\t// Returns the entry index: the module walk and the nested declare both append entries of their\n\t\t// own, so neither end of the list identifies this one. The caller hands over configs — the entry\n\t\t// keeps it, so it must not be pooled or reused.\n\t\tint RegisterEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimelineNodes(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n// … (truncated)"
     }
    },
    {
     "line": 58,
     "state": {
      "comment": "// Definition asset -> its key, for assets already declared. Also the re-entry guard: a reference\n// cycle resolves to the key instead of recursing.",
      "annotated_code": "\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();\n\n\t\tint _generatedCounter;\n\t\tint _timelineCount;\n\n\t\t// Declare phase — before world construction. Scene compositions and every collection definition\n\t\t// register their archetypes (a definition gets a template whether or not a scene references it,\n\t\t// so any spawn-by-key has a group to resolve).\n\t\tinternal void Declare(Initializer[] sceneInitializers, EntityDefinitionCollection collection = null)\n\t\t{\n\t\t\tforeach (var initializer in sceneInitializers)\n\t\t\t\tif (initializer is EntityInitializer entityInitializer)\n\t\t\t\t\tentityInitializer.DeclareTemplate(this);\n\n\t\t\tDeclareDefinitions(collection);\n\t\t}\n\n\t\t// Feeds the registered archetypes to the WorldBuilder; call once after Declare.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\t// Keyless entry point, for a scene composition that bakes transient.\n\t\tinternal TemplateRegistry.RegisteredArchetype RegisterComposition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = RegisterEntry(nameHint, configs, nested);\n\t\t\treturn _entries[entry].Archetype;\n\t\t}\n\n\t\t// Keyed entry point: same registration, plus the key -> entry lookup spawn resolves through.\n\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = RegisterEntry(nameHint, configs, nested);\n\n\t\t// Every composition funnels through here — one place that walks the modules, records the entry\n\t\t// and declares the nested defs, so no declare site can register a composition and forget half of\n\t\t// it. AbilityOwner and ActionTimeline carry references to other definitions — declare each\n\t\t// (minting a generated key for inline ones) and stamp the key back onto the config for Bake to\n\t\t// read. Augments configs in place, so the caller's list is the effective composition — the same\n\t\t// one that must reach Bake, or the template carries components the blob has no payload for.\n\t\t// Returns the entry index: the module walk and the nested declare both append entries of their\n\t\t// own, so neither end of the list identifies this one. The caller hands over configs — the entry\n\t\t// keeps it, so it must not be pooled or reused.\n\t\tint RegisterEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimelineNodes(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = DeclareEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n// … (truncated)"
     }
    },
    {
     "line": 65,
     "state": {
      "comment": "// Declare phase — before world construction. Scene compositions and every collection definition\n// register their archetypes (a definition gets a template whether or not a scene references it,\n// so any spawn-by-key has a group to resolve).",
      "annotated_code": "\t\tinternal void Declare(Initializer[] sceneInitializers, EntityDefinitionCollection collection = null)\n\t\t{\n\t\t\tforeach (var initializer in sceneInitializers)\n\t\t\t\tif (initializer is EntityInitializer entityInitializer)\n\t\t\t\t\tentityInitializer.DeclareTemplate(this);\n\n\t\t\tDeclareDefinitions(collection);\n\t\t}\n\n\t\t// Feeds the registered archetypes to the WorldBuilder; call once after Declare.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\t// Keyless entry point, for a scene composition that bakes transient.\n\t\tinternal TemplateRegistry.RegisteredArchetype RegisterComposition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = RegisterEntry(nameHint, configs, nested);\n\t\t\treturn _entries[entry].Archetype;\n\t\t}\n\n\t\t// Keyed entry point: same registration, plus the key -> entry lookup spawn resolves through.\n\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = RegisterEntry(nameHint, configs, nested);\n\n\t\t// Every composition funnels through here — one place that walks the modules, records the entry\n\t\t// and declares the nested defs, so no declare site can register a composition and forget half of\n\t\t// it. AbilityOwner and ActionTimeline carry references to other definitions — declare each\n\t\t// (minting a generated key for inline ones) and stamp the key back onto the config for Bake to\n\t\t// read. Augments configs in place, so the caller's list is the effective composition — the same\n\t\t// one that must reach Bake, or the template carries components the blob has no payload for.\n\t\t// Returns the entry index: the module walk and the nested declare both append entries of their\n\t\t// own, so neither end of the list identifies this one. The caller hands over configs — the entry\n\t\t// keeps it, so it must not be pooled or reused.\n\t\tint RegisterEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimelineNodes(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = DeclareEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _entries.Count;\n\t\t\t_entries.Add(new Entry(nameHint, configs, nested ?? NoNested, archetype));\n\t\t\tDeclareNested(nested);\n\t\t\treturn index;\n// … (truncated)"
     }
    },
    {
     "line": 77,
     "state": {
      "comment": "// Feeds the registered archetypes to the WorldBuilder; call once after Declare.",
      "annotated_code": "\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\t// Keyless entry point, for a scene composition that bakes transient.\n\t\tinternal TemplateRegistry.RegisteredArchetype RegisterComposition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = RegisterEntry(nameHint, configs, nested);\n\t\t\treturn _entries[entry].Archetype;\n\t\t}\n\n\t\t// Keyed entry point: same registration, plus the key -> entry lookup spawn resolves through.\n\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = RegisterEntry(nameHint, configs, nested);\n\n\t\t// Every composition funnels through here — one place that walks the modules, records the entry\n\t\t// and declares the nested defs, so no declare site can register a composition and forget half of\n\t\t// it. AbilityOwner and ActionTimeline carry references to other definitions — declare each\n\t\t// (minting a generated key for inline ones) and stamp the key back onto the config for Bake to\n\t\t// read. Augments configs in place, so the caller's list is the effective composition — the same\n\t\t// one that must reach Bake, or the template carries components the blob has no payload for.\n\t\t// Returns the entry index: the module walk and the nested declare both append entries of their\n\t\t// own, so neither end of the list identifies this one. The caller hands over configs — the entry\n\t\t// keeps it, so it must not be pooled or reused.\n\t\tint RegisterEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimelineNodes(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = DeclareEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _entries.Count;\n\t\t\t_entries.Add(new Entry(nameHint, configs, nested ?? NoNested, archetype));\n\t\t\tDeclareNested(nested);\n\t\t\treturn index;\n\t\t}\n\n\t\t// An ability is an ordinary definition asset (the Ability contract is what marks it castable), so\n\t\t// it declares through the shared path and shares the asset's own key with every other reference\n\t\t// to it.\n\t\tvoid DeclareAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = Declare(owner.Ordered);\n\t\t\towner.ResolvedAuto = Declare(owner.Auto);\n\n// … (truncated)"
     }
    },
    {
     "line": 80,
     "state": {
      "comment": "// Keyless entry point, for a scene composition that bakes transient.",
      "annotated_code": "\t\tinternal TemplateRegistry.RegisteredArchetype RegisterComposition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = RegisterEntry(nameHint, configs, nested);\n\t\t\treturn _entries[entry].Archetype;\n\t\t}\n\n\t\t// Keyed entry point: same registration, plus the key -> entry lookup spawn resolves through.\n\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = RegisterEntry(nameHint, configs, nested);\n\n\t\t// Every composition funnels through here — one place that walks the modules, records the entry\n\t\t// and declares the nested defs, so no declare site can register a composition and forget half of\n\t\t// it. AbilityOwner and ActionTimeline carry references to other definitions — declare each\n\t\t// (minting a generated key for inline ones) and stamp the key back onto the config for Bake to\n\t\t// read. Augments configs in place, so the caller's list is the effective composition — the same\n\t\t// one that must reach Bake, or the template carries components the blob has no payload for.\n\t\t// Returns the entry index: the module walk and the nested declare both append entries of their\n\t\t// own, so neither end of the list identifies this one. The caller hands over configs — the entry\n\t\t// keeps it, so it must not be pooled or reused.\n\t\tint RegisterEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimelineNodes(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = DeclareEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _entries.Count;\n\t\t\t_entries.Add(new Entry(nameHint, configs, nested ?? NoNested, archetype));\n\t\t\tDeclareNested(nested);\n\t\t\treturn index;\n\t\t}\n\n\t\t// An ability is an ordinary definition asset (the Ability contract is what marks it castable), so\n\t\t// it declares through the shared path and shares the asset's own key with every other reference\n\t\t// to it.\n\t\tvoid DeclareAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = Declare(owner.Ordered);\n\t\t\towner.ResolvedAuto = Declare(owner.Auto);\n\n\t\t\tEntityDefKey[] Declare(EntityDefinitionAsset[] list)\n\t\t\t{\n\t\t\t\tvar keys = new EntityDefKey[list.Length];\n// … (truncated)"
     }
    },
    {
     "line": 87,
     "state": {
      "comment": "// Keyed entry point: same registration, plus the key -> entry lookup spawn resolves through.",
      "annotated_code": "\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = RegisterEntry(nameHint, configs, nested);\n\n\t\t// Every composition funnels through here — one place that walks the modules, records the entry\n\t\t// and declares the nested defs, so no declare site can register a composition and forget half of\n\t\t// it. AbilityOwner and ActionTimeline carry references to other definitions — declare each\n\t\t// (minting a generated key for inline ones) and stamp the key back onto the config for Bake to\n\t\t// read. Augments configs in place, so the caller's list is the effective composition — the same\n\t\t// one that must reach Bake, or the template carries components the blob has no payload for.\n\t\t// Returns the entry index: the module walk and the nested declare both append entries of their\n\t\t// own, so neither end of the list identifies this one. The caller hands over configs — the entry\n\t\t// keeps it, so it must not be pooled or reused.\n\t\tint RegisterEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimelineNodes(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = DeclareEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _entries.Count;\n\t\t\t_entries.Add(new Entry(nameHint, configs, nested ?? NoNested, archetype));\n\t\t\tDeclareNested(nested);\n\t\t\treturn index;\n\t\t}\n\n\t\t// An ability is an ordinary definition asset (the Ability contract is what marks it castable), so\n\t\t// it declares through the shared path and shares the asset's own key with every other reference\n\t\t// to it.\n\t\tvoid DeclareAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = Declare(owner.Ordered);\n\t\t\towner.ResolvedAuto = Declare(owner.Auto);\n\n\t\t\tEntityDefKey[] Declare(EntityDefinitionAsset[] list)\n\t\t\t{\n\t\t\t\tvar keys = new EntityDefKey[list.Length];\n\t\t\t\tfor (int i = 0; i < list.Length; i++)\n\t\t\t\t\tif (list[i] != null)\n\t\t\t\t\t\tkeys[i] = DeclareDefinitionAsset(list[i]);\n\t\t\t\treturn keys;\n\t\t\t}\n\t\t}\n\n// … (truncated)"
     }
    },
    {
     "line": 91,
     "state": {
      "comment": "// Every composition funnels through here — one place that walks the modules, records the entry\n// and declares the nested defs, so no declare site can register a composition and forget half of\n// it. AbilityOwner and ActionTimeline carry references to other definitions — declare each\n// (minting a generated key for inline ones) and stamp the key back onto the config for Bake to\n// read. Augments configs in place, so the caller's list is the effective composition — the same\n// one that must reach Bake, or the template carries components the blob has no payload for.\n// Returns the entry index: the module walk and the nested declare both append entries of their\n// own, so neither end of the list identifies this one. The caller hands over configs — the entry\n// keeps it, so it must not be pooled or reused.",
      "annotated_code": "\t\tint RegisterEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimelineNodes(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = DeclareEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _entries.Count;\n\t\t\t_entries.Add(new Entry(nameHint, configs, nested ?? NoNested, archetype));\n\t\t\tDeclareNested(nested);\n\t\t\treturn index;\n\t\t}\n\n\t\t// An ability is an ordinary definition asset (the Ability contract is what marks it castable), so\n\t\t// it declares through the shared path and shares the asset's own key with every other reference\n\t\t// to it.\n\t\tvoid DeclareAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = Declare(owner.Ordered);\n\t\t\towner.ResolvedAuto = Declare(owner.Auto);\n\n\t\t\tEntityDefKey[] Declare(EntityDefinitionAsset[] list)\n\t\t\t{\n\t\t\t\tvar keys = new EntityDefKey[list.Length];\n\t\t\t\tfor (int i = 0; i < list.Length; i++)\n\t\t\t\t\tif (list[i] != null)\n\t\t\t\t\t\tkeys[i] = DeclareDefinitionAsset(list[i]);\n\t\t\t\treturn keys;\n\t\t\t}\n\t\t}\n\n\t\t// An external ref reuses the asset's own key: unlike a timeline node there is no per-site override,\n\t\t// so minting a fresh key would duplicate the archetype and its blob for nothing. Inline\n\t\t// definitions get a generated key, deduped by config identity so one config declares once —\n\t\t// the first declare site's path names it, since one shared config has no single owner.\n\t\tEntityDefKey DeclareEntityCreate(EntityCreateConfig config, string context)\n\t\t{\n\t\t\tif (config.DefinitionAsset != null)\n\t\t\t\treturn DeclareDefinitionAsset(config.DefinitionAsset);\n\n\t\t\tif (_inlineCreateKeys.TryGetValue(config, out var existing))\n\t\t\t\treturn existing;\n\n// … (truncated)"
     }
    },
    {
     "line": 130,
     "state": {
      "comment": "// An ability is an ordinary definition asset (the Ability contract is what marks it castable), so\n// it declares through the shared path and shares the asset's own key with every other reference\n// to it.",
      "annotated_code": "\t\tvoid DeclareAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = Declare(owner.Ordered);\n\t\t\towner.ResolvedAuto = Declare(owner.Auto);\n\n\t\t\tEntityDefKey[] Declare(EntityDefinitionAsset[] list)\n\t\t\t{\n\t\t\t\tvar keys = new EntityDefKey[list.Length];\n\t\t\t\tfor (int i = 0; i < list.Length; i++)\n\t\t\t\t\tif (list[i] != null)\n\t\t\t\t\t\tkeys[i] = DeclareDefinitionAsset(list[i]);\n\t\t\t\treturn keys;\n\t\t\t}\n\t\t}\n\n\t\t// An external ref reuses the asset's own key: unlike a timeline node there is no per-site override,\n\t\t// so minting a fresh key would duplicate the archetype and its blob for nothing. Inline\n\t\t// definitions get a generated key, deduped by config identity so one config declares once —\n\t\t// the first declare site's path names it, since one shared config has no single owner.\n\t\tEntityDefKey DeclareEntityCreate(EntityCreateConfig config, string context)\n\t\t{\n\t\t\tif (config.DefinitionAsset != null)\n\t\t\t\treturn DeclareDefinitionAsset(config.DefinitionAsset);\n\n\t\t\tif (_inlineCreateKeys.TryGetValue(config, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = config.InlineDefinition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\t\t\tif (def.Modules == null || def.Modules.Count == 0)\n\t\t\t\treturn default; // Validate reports it; nothing to declare\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_inlineCreateKeys[config] = key; // set before recursion — guards a definition spawning itself\n\t\t\tDeclareKeyed(key, context, configs, def.Nested);\n\t\t\treturn key;\n\t\t}\n\n\t\t// A referenced definition asset declares itself, so reachability is the membership rule — nothing\n\t\t// has to be listed anywhere for a reference to resolve. The collection only adds the roots\n\t\t// nothing references statically (spawn-by-key from code or a save).\n\t\tinternal EntityDefKey DeclareDefinitionAsset(EntityDefinitionAsset asset)\n\t\t{\n\t\t\tAssert.IsNotNull(asset, \"declare definition: null asset\");\n\t\t\tif (_assetKeys.TryGetValue(asset, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar definition = asset.Definition;\n\t\t\tAssert.IsNotNull(definition, $\"'{asset.name}': empty definition\");\n\t\t\tAssert.IsFalse(definition.Id.IsNone, $\"'{asset.name}': definition without id\");\n\t\t\tAssert.IsFalse(definition.Id.IsGenerated, $\"'{asset.name}': asset id in the compiler-minted range\");\n\t\t\tAssert.IsFalse(_byKey.ContainsKey(definition.Id), $\"'{asset.name}': duplicate definition id {definition.Id}\");\n\n\t\t\t_assetKeys[asset] = definition.Id; // set before recursion — guards a definition reaching itself\n\n\t\t\t// Copy: the declare augments the list, and the asset's own is serialized data.\n\t\t\tvar configs = new List<ModuleConfig>(definition.Modules);\n\t\t\tDeclareKeyed(definition.Id, asset.name, configs, definition.Nested);\n\t\t\treturn definition.Id;\n// … (truncated)"
     }
    },
    {
     "line": 148,
     "state": {
      "comment": "// An external ref reuses the asset's own key: unlike a timeline node there is no per-site override,\n// so minting a fresh key would duplicate the archetype and its blob for nothing. Inline\n// definitions get a generated key, deduped by config identity so one config declares once —\n// the first declare site's path names it, since one shared config has no single owner.",
      "annotated_code": "\t\tEntityDefKey DeclareEntityCreate(EntityCreateConfig config, string context)\n\t\t{\n\t\t\tif (config.DefinitionAsset != null)\n\t\t\t\treturn DeclareDefinitionAsset(config.DefinitionAsset);\n\n\t\t\tif (_inlineCreateKeys.TryGetValue(config, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = config.InlineDefinition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\t\t\tif (def.Modules == null || def.Modules.Count == 0)\n\t\t\t\treturn default; // Validate reports it; nothing to declare\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_inlineCreateKeys[config] = key; // set before recursion — guards a definition spawning itself\n\t\t\tDeclareKeyed(key, context, configs, def.Nested);\n\t\t\treturn key;\n\t\t}\n\n\t\t// A referenced definition asset declares itself, so reachability is the membership rule — nothing\n\t\t// has to be listed anywhere for a reference to resolve. The collection only adds the roots\n\t\t// nothing references statically (spawn-by-key from code or a save).\n\t\tinternal EntityDefKey DeclareDefinitionAsset(EntityDefinitionAsset asset)\n\t\t{\n\t\t\tAssert.IsNotNull(asset, \"declare definition: null asset\");\n\t\t\tif (_assetKeys.TryGetValue(asset, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar definition = asset.Definition;\n\t\t\tAssert.IsNotNull(definition, $\"'{asset.name}': empty definition\");\n\t\t\tAssert.IsFalse(definition.Id.IsNone, $\"'{asset.name}': definition without id\");\n\t\t\tAssert.IsFalse(definition.Id.IsGenerated, $\"'{asset.name}': asset id in the compiler-minted range\");\n\t\t\tAssert.IsFalse(_byKey.ContainsKey(definition.Id), $\"'{asset.name}': duplicate definition id {definition.Id}\");\n\n\t\t\t_assetKeys[asset] = definition.Id; // set before recursion — guards a definition reaching itself\n\n\t\t\t// Copy: the declare augments the list, and the asset's own is serialized data.\n\t\t\tvar configs = new List<ModuleConfig>(definition.Modules);\n\t\t\tDeclareKeyed(definition.Id, asset.name, configs, definition.Nested);\n\t\t\treturn definition.Id;\n\t\t}\n\n\t\t// Nested defs spawn by key, so each must be declared — including from a scene initializer, whose\n\t\t// transient blob bakes its definition's nested list without declaring the definition itself.\n\t\tvoid DeclareNested(IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tif (nested == null)\n\t\t\t\treturn;\n\t\t\tforeach (var n in nested)\n\t\t\t\tif (n.DefinitionAssetRef != null)\n\t\t\t\t\tDeclareDefinitionAsset(n.DefinitionAssetRef);\n\t\t}\n\n\t\tvoid DeclareTimelineNodes(ActionTimelineModuleConfig timeline, string context)\n\t\t{\n\t\t\t_timelineCount++;\n\t\t\tfor (int i = 0; i < timeline.Nodes.Count; i++)\n\t\t\t{\n\t\t\t\tvar node = timeline.Nodes[i];\n// … (truncated)"
     }
    },
    {
     "line": 172,
     "state": {
      "comment": "// A referenced definition asset declares itself, so reachability is the membership rule — nothing\n// has to be listed anywhere for a reference to resolve. The collection only adds the roots\n// nothing references statically (spawn-by-key from code or a save).",
      "annotated_code": "\t\tinternal EntityDefKey DeclareDefinitionAsset(EntityDefinitionAsset asset)\n\t\t{\n\t\t\tAssert.IsNotNull(asset, \"declare definition: null asset\");\n\t\t\tif (_assetKeys.TryGetValue(asset, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar definition = asset.Definition;\n\t\t\tAssert.IsNotNull(definition, $\"'{asset.name}': empty definition\");\n\t\t\tAssert.IsFalse(definition.Id.IsNone, $\"'{asset.name}': definition without id\");\n\t\t\tAssert.IsFalse(definition.Id.IsGenerated, $\"'{asset.name}': asset id in the compiler-minted range\");\n\t\t\tAssert.IsFalse(_byKey.ContainsKey(definition.Id), $\"'{asset.name}': duplicate definition id {definition.Id}\");\n\n\t\t\t_assetKeys[asset] = definition.Id; // set before recursion — guards a definition reaching itself\n\n\t\t\t// Copy: the declare augments the list, and the asset's own is serialized data.\n\t\t\tvar configs = new List<ModuleConfig>(definition.Modules);\n\t\t\tDeclareKeyed(definition.Id, asset.name, configs, definition.Nested);\n\t\t\treturn definition.Id;\n\t\t}\n\n\t\t// Nested defs spawn by key, so each must be declared — including from a scene initializer, whose\n\t\t// transient blob bakes its definition's nested list without declaring the definition itself.\n\t\tvoid DeclareNested(IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tif (nested == null)\n\t\t\t\treturn;\n\t\t\tforeach (var n in nested)\n\t\t\t\tif (n.DefinitionAssetRef != null)\n\t\t\t\t\tDeclareDefinitionAsset(n.DefinitionAssetRef);\n\t\t}\n\n\t\tvoid DeclareTimelineNodes(ActionTimelineModuleConfig timeline, string context)\n\t\t{\n\t\t\t_timelineCount++;\n\t\t\tfor (int i = 0; i < timeline.Nodes.Count; i++)\n\t\t\t{\n\t\t\t\tvar node = timeline.Nodes[i];\n\t\t\t\tAssert.IsNotNull(node, $\"'{context}': empty node entry\");\n\t\t\t\tnode.ResolvedDefinition = DeclareTimelineNode(node, $\"{context}[{i}]\");\n\t\t\t}\n\t\t}\n\n\t\t// A node's def is an inline definition wrapped as a time-gated reaction: its modules plus an\n\t\t// ensured ConditionTime (the framework's time gate; ActionTimelineSystem overrides the window\n\t\t// per cast — the reaction core follows from it via ModuleClosure). Always a fresh generated key\n\t\t// — even an external ref is copied here so the shared collection def keeps its ungated shape\n\t\t// (per-node overrides come later).\n\t\tEntityDefKey DeclareTimelineNode(TimelineNodeConfig node, string context)\n\t\t{\n\t\t\tif (_nodeKeys.TryGetValue(node, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = node.Definition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tModuleClosure.Ensure(configs, EntityModules.ConditionTime, () => new ConditionTimeModuleConfig());\n\n\t\t\t// A node is spawned with no placement (ActionTimelineSystem has no anchor to offer), so a body\n\t\t\t// would fail its transform check on every cast. The node's own actions place what they spawn.\n// … (truncated)"
     }
    },
    {
     "line": 189,
     "state": {
      "comment": "// Copy: the declare augments the list, and the asset's own is serialized data.",
      "annotated_code": "\t\t\tvar configs = new List<ModuleConfig>(definition.Modules);\n\t\t\tDeclareKeyed(definition.Id, asset.name, configs, definition.Nested);\n\t\t\treturn definition.Id;"
     }
    },
    {
     "line": 195,
     "state": {
      "comment": "// Nested defs spawn by key, so each must be declared — including from a scene initializer, whose\n// transient blob bakes its definition's nested list without declaring the definition itself.",
      "annotated_code": "\t\tvoid DeclareNested(IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tif (nested == null)\n\t\t\t\treturn;\n\t\t\tforeach (var n in nested)\n\t\t\t\tif (n.DefinitionAssetRef != null)\n\t\t\t\t\tDeclareDefinitionAsset(n.DefinitionAssetRef);\n\t\t}\n\n\t\tvoid DeclareTimelineNodes(ActionTimelineModuleConfig timeline, string context)\n\t\t{\n\t\t\t_timelineCount++;\n\t\t\tfor (int i = 0; i < timeline.Nodes.Count; i++)\n\t\t\t{\n\t\t\t\tvar node = timeline.Nodes[i];\n\t\t\t\tAssert.IsNotNull(node, $\"'{context}': empty node entry\");\n\t\t\t\tnode.ResolvedDefinition = DeclareTimelineNode(node, $\"{context}[{i}]\");\n\t\t\t}\n\t\t}\n\n\t\t// A node's def is an inline definition wrapped as a time-gated reaction: its modules plus an\n\t\t// ensured ConditionTime (the framework's time gate; ActionTimelineSystem overrides the window\n\t\t// per cast — the reaction core follows from it via ModuleClosure). Always a fresh generated key\n\t\t// — even an external ref is copied here so the shared collection def keeps its ungated shape\n\t\t// (per-node overrides come later).\n\t\tEntityDefKey DeclareTimelineNode(TimelineNodeConfig node, string context)\n\t\t{\n\t\t\tif (_nodeKeys.TryGetValue(node, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = node.Definition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tModuleClosure.Ensure(configs, EntityModules.ConditionTime, () => new ConditionTimeModuleConfig());\n\n\t\t\t// A node is spawned with no placement (ActionTimelineSystem has no anchor to offer), so a body\n\t\t\t// would fail its transform check on every cast. The node's own actions place what they spawn.\n\t\t\tAssert.IsFalse(HasTransform(configs), $\"'{context}': timeline node is placed, but a node is spawned without a placement\");\n\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_nodeKeys[node] = key; // set before recursion — guards re-entry\n\t\t\tDeclareKeyed(key, node.DefinitionAsset != null ? $\"{context} ▸ {node.DefinitionAsset.name}\" : context, configs, def.Nested);\n\t\t\treturn key;\n\t\t}\n\n\t\tvoid DeclareDefinitions(EntityDefinitionCollection collection)\n\t\t{\n\t\t\tif (collection == null)\n\t\t\t\treturn;\n\n\t\t\tforeach (var asset in collection.Definitions)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(asset, $\"'{collection.name}': empty definition entry\");\n\t\t\t\tDeclareDefinitionAsset(asset);\n\t\t\t}\n\t\t}\n\n\t\tEntityDefKey MintGeneratedKey() => EntityDefKey.Generated(_generatedCounter++);\n\n// … (truncated)"
     }
    },
    {
     "line": 217,
     "state": {
      "comment": "// A node's def is an inline definition wrapped as a time-gated reaction: its modules plus an\n// ensured ConditionTime (the framework's time gate; ActionTimelineSystem overrides the window\n// per cast — the reaction core follows from it via ModuleClosure). Always a fresh generated key\n// — even an external ref is copied here so the shared collection def keeps its ungated shape\n// (per-node overrides come later).",
      "annotated_code": "\t\tEntityDefKey DeclareTimelineNode(TimelineNodeConfig node, string context)\n\t\t{\n\t\t\tif (_nodeKeys.TryGetValue(node, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = node.Definition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tModuleClosure.Ensure(configs, EntityModules.ConditionTime, () => new ConditionTimeModuleConfig());\n\n\t\t\t// A node is spawned with no placement (ActionTimelineSystem has no anchor to offer), so a body\n\t\t\t// would fail its transform check on every cast. The node's own actions place what they spawn.\n\t\t\tAssert.IsFalse(HasTransform(configs), $\"'{context}': timeline node is placed, but a node is spawned without a placement\");\n\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_nodeKeys[node] = key; // set before recursion — guards re-entry\n\t\t\tDeclareKeyed(key, node.DefinitionAsset != null ? $\"{context} ▸ {node.DefinitionAsset.name}\" : context, configs, def.Nested);\n\t\t\treturn key;\n\t\t}\n\n\t\tvoid DeclareDefinitions(EntityDefinitionCollection collection)\n\t\t{\n\t\t\tif (collection == null)\n\t\t\t\treturn;\n\n\t\t\tforeach (var asset in collection.Definitions)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(asset, $\"'{collection.name}': empty definition entry\");\n\t\t\t\tDeclareDefinitionAsset(asset);\n\t\t\t}\n\t\t}\n\n\t\tEntityDefKey MintGeneratedKey() => EntityDefKey.Generated(_generatedCounter++);\n\n\t\t// By entry, null for none. A look shared by several definitions is walked once.\n\t\tpublic IReadOnlyList<LookSource> BuildLooks()\n\t\t{\n\t\t\tvar walked = new Dictionary<Look, LookSource>();\n\t\t\tvar looks = new LookSource[_entries.Count];\n\t\t\tfor (int i = 0; i < _entries.Count; i++)\n\t\t\t\tforeach (var config in _entries[i].Modules)\n\t\t\t\t{\n\t\t\t\t\tif (config is not LookModuleConfig lookConfig || lookConfig.Look == null)\n\t\t\t\t\t\tcontinue;\n\n\t\t\t\t\tif (!walked.TryGetValue(lookConfig.Look, out var source))\n\t\t\t\t\t\twalked.Add(lookConfig.Look, source = lookConfig.Look.Walk());\n\t\t\t\t\tlooks[i] = source;\n\t\t\t\t}\n\n\t\t\treturn looks;\n\t\t}\n\n\t\t// Publishes every declared definition (collection + generated inline) into the\n\t\t// CDefinitionRegistry global, reusing the Declare archetypes. Each ActionTimeline module bakes\n\t\t// its TimelineBlob as a side-effect (into _timelines). The global's create/add/dispose live on\n\t\t// DefinitionRegistry (owns the accessor + world-lifetime teardown); this only drives the\n\t\t// iteration over the keys it holds. Runs at init after the world is built.\n\t\tpublic void BuildBlobRegistry(DefinitionRegistry definitions)\n// … (truncated)"
     }
    },
    {
     "line": 233,
     "state": {
      "comment": "// A node is spawned with no placement (ActionTimelineSystem has no anchor to offer), so a body\n// would fail its transform check on every cast. The node's own actions place what they spawn.",
      "annotated_code": "\t\t\tAssert.IsFalse(HasTransform(configs), $\"'{context}': timeline node is placed, but a node is spawned without a placement\");\n\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_nodeKeys[node] = key; // set before recursion — guards re-entry\n\t\t\tDeclareKeyed(key, node.DefinitionAsset != null ? $\"{context} ▸ {node.DefinitionAsset.name}\" : context, configs, def.Nested);\n\t\t\treturn key;"
     }
    },
    {
     "line": 257,
     "state": {
      "comment": "// By entry, null for none. A look shared by several definitions is walked once.",
      "annotated_code": "\t\tpublic IReadOnlyList<LookSource> BuildLooks()\n\t\t{\n\t\t\tvar walked = new Dictionary<Look, LookSource>();\n\t\t\tvar looks = new LookSource[_entries.Count];\n\t\t\tfor (int i = 0; i < _entries.Count; i++)\n\t\t\t\tforeach (var config in _entries[i].Modules)\n\t\t\t\t{\n\t\t\t\t\tif (config is not LookModuleConfig lookConfig || lookConfig.Look == null)\n\t\t\t\t\t\tcontinue;\n\n\t\t\t\t\tif (!walked.TryGetValue(lookConfig.Look, out var source))\n\t\t\t\t\t\twalked.Add(lookConfig.Look, source = lookConfig.Look.Walk());\n\t\t\t\t\tlooks[i] = source;\n\t\t\t\t}\n\n\t\t\treturn looks;\n\t\t}\n\n\t\t// Publishes every declared definition (collection + generated inline) into the\n\t\t// CDefinitionRegistry global, reusing the Declare archetypes. Each ActionTimeline module bakes\n\t\t// its TimelineBlob as a side-effect (into _timelines). The global's create/add/dispose live on\n\t\t// DefinitionRegistry (owns the accessor + world-lifetime teardown); this only drives the\n\t\t// iteration over the keys it holds. Runs at init after the world is built.\n\t\tpublic void BuildBlobRegistry(DefinitionRegistry definitions)\n\t\t{\n\t\t\tValidateNested();\n\t\t\tdefinitions.CreateDefinitionRegistry(_byKey.Count, _timelineCount);\n\t\t\tforeach (var kvp in _byKey)\n\t\t\t{\n\t\t\t\tvar entry = _entries[kvp.Value];\n\t\t\t\tdefinitions.AddEntityDefinition(kvp.Key, kvp.Value, entry.Name, entry.Modules, entry.Nested, entry.Archetype.TagSet);\n\t\t\t}\n\t\t}\n\n\t\t// Two checks over the declared graph. A nested def that needs a transform requires the parent to\n\t\t// have one too — the nested def inherits the parent transform, spawn has nothing else to anchor it\n\t\t// (transform-as-override is deferred). Over every entry, not just the keyed ones: a scene\n\t\t// composition has no key but nests by key like any other parent.\n\t\t// Then the spawn-recursion cycle check below.\n\t\tvoid ValidateNested()\n\t\t{\n\t\t\tforeach (var entry in _entries)\n\t\t\t{\n\t\t\t\tbool parentIsPlaced = HasTransform(entry.Modules);\n\t\t\t\tforeach (var n in entry.Nested)\n\t\t\t\t{\n\t\t\t\t\tvar nd = n.DefinitionAssetRef;\n\t\t\t\t\tif (nd == null)\n\t\t\t\t\t\tcontinue;\n\t\t\t\t\tvar def = nd.Definition;\n\t\t\t\t\tAssert.IsTrue(_byKey.ContainsKey(def.Id), $\"'{entry.Name}': nested '{def.Name}' was not declared\");\n\t\t\t\t\tAssert.IsTrue(parentIsPlaced || !HasTransform(def.Modules), $\"'{entry.Name}': nests placed '{def.Name}' but has no transform to anchor it\");\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// One spawn recurses into nested defs and abilities, so a cycle there would recurse forever —\n\t\t\t// reject at blob-build time. Timeline nodes are not on this graph: they spawn later, through\n\t\t\t// the request queue, so a loop among them is a runaway rather than a stack overflow.\n\t\t\t// Keyed entries only: a scene composition is never referenced, so it can't be on a cycle.\n\t\t\tvar state = new Dictionary<EntityDefKey, byte>(_byKey.Count);\n// … (truncated)"
     }
    },
    {
     "line": 276,
     "state": {
      "comment": "// Publishes every declared definition (collection + generated inline) into the\n// CDefinitionRegistry global, reusing the Declare archetypes. Each ActionTimeline module bakes\n// its TimelineBlob as a side-effect (into _timelines). The global's create/add/dispose live on\n// DefinitionRegistry (owns the accessor + world-lifetime teardown); this only drives the\n// iteration over the keys it holds. Runs at init after the world is built.",
      "annotated_code": "\t\tpublic void BuildBlobRegistry(DefinitionRegistry definitions)\n\t\t{\n\t\t\tValidateNested();\n\t\t\tdefinitions.CreateDefinitionRegistry(_byKey.Count, _timelineCount);\n\t\t\tforeach (var kvp in _byKey)\n\t\t\t{\n\t\t\t\tvar entry = _entries[kvp.Value];\n\t\t\t\tdefinitions.AddEntityDefinition(kvp.Key, kvp.Value, entry.Name, entry.Modules, entry.Nested, entry.Archetype.TagSet);\n\t\t\t}\n\t\t}\n\n\t\t// Two checks over the declared graph. A nested def that needs a transform requires the parent to\n\t\t// have one too — the nested def inherits the parent transform, spawn has nothing else to anchor it\n\t\t// (transform-as-override is deferred). Over every entry, not just the keyed ones: a scene\n\t\t// composition has no key but nests by key like any other parent.\n\t\t// Then the spawn-recursion cycle check below.\n\t\tvoid ValidateNested()\n\t\t{\n\t\t\tforeach (var entry in _entries)\n\t\t\t{\n\t\t\t\tbool parentIsPlaced = HasTransform(entry.Modules);\n\t\t\t\tforeach (var n in entry.Nested)\n\t\t\t\t{\n\t\t\t\t\tvar nd = n.DefinitionAssetRef;\n\t\t\t\t\tif (nd == null)\n\t\t\t\t\t\tcontinue;\n\t\t\t\t\tvar def = nd.Definition;\n\t\t\t\t\tAssert.IsTrue(_byKey.ContainsKey(def.Id), $\"'{entry.Name}': nested '{def.Name}' was not declared\");\n\t\t\t\t\tAssert.IsTrue(parentIsPlaced || !HasTransform(def.Modules), $\"'{entry.Name}': nests placed '{def.Name}' but has no transform to anchor it\");\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// One spawn recurses into nested defs and abilities, so a cycle there would recurse forever —\n\t\t\t// reject at blob-build time. Timeline nodes are not on this graph: they spawn later, through\n\t\t\t// the request queue, so a loop among them is a runaway rather than a stack overflow.\n\t\t\t// Keyed entries only: a scene composition is never referenced, so it can't be on a cycle.\n\t\t\tvar state = new Dictionary<EntityDefKey, byte>(_byKey.Count);\n\t\t\tforeach (var kvp in _byKey)\n\t\t\t\tDetectSpawnCycle(kvp.Key, state);\n\t\t}\n\n\t\t// DFS colouring: revisiting an in-stack (grey = 1) node is a back edge ⇒ cycle; 2 = done.\n\t\tvoid DetectSpawnCycle(EntityDefKey key, Dictionary<EntityDefKey, byte> state)\n\t\t{\n\t\t\tvar entry = _entries[_byKey[key]];\n\t\t\tstate.TryGetValue(key, out var mark);\n\t\t\tAssert.IsTrue(mark != 1, $\"definition spawn cycle through '{entry.Name}'\");\n\t\t\tif (mark == 2)\n\t\t\t\treturn;\n\n\t\t\tstate[key] = 1;\n\t\t\tforeach (var nested in entry.Nested)\n\t\t\t{\n\t\t\t\tvar n = nested.DefinitionAssetRef?.Definition;\n\t\t\t\tif (n != null && _byKey.ContainsKey(n.Id))\n\t\t\t\t\tDetectSpawnCycle(n.Id, state);\n\t\t\t}\n\t\t\tforeach (var config in entry.Modules)\n\t\t\t{\n\t\t\t\tif (config is not AbilityOwnerModuleConfig owner)\n// … (truncated)"
     }
    },
    {
     "line": 292,
     "state": {
      "comment": "// Two checks over the declared graph. A nested def that needs a transform requires the parent to\n// have one too — the nested def inherits the parent transform, spawn has nothing else to anchor it\n// (transform-as-override is deferred). Over every entry, not just the keyed ones: a scene\n// composition has no key but nests by key like any other parent.\n// Then the spawn-recursion cycle check below.",
      "annotated_code": "\t\tvoid ValidateNested()\n\t\t{\n\t\t\tforeach (var entry in _entries)\n\t\t\t{\n\t\t\t\tbool parentIsPlaced = HasTransform(entry.Modules);\n\t\t\t\tforeach (var n in entry.Nested)\n\t\t\t\t{\n\t\t\t\t\tvar nd = n.DefinitionAssetRef;\n\t\t\t\t\tif (nd == null)\n\t\t\t\t\t\tcontinue;\n\t\t\t\t\tvar def = nd.Definition;\n\t\t\t\t\tAssert.IsTrue(_byKey.ContainsKey(def.Id), $\"'{entry.Name}': nested '{def.Name}' was not declared\");\n\t\t\t\t\tAssert.IsTrue(parentIsPlaced || !HasTransform(def.Modules), $\"'{entry.Name}': nests placed '{def.Name}' but has no transform to anchor it\");\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// One spawn recurses into nested defs and abilities, so a cycle there would recurse forever —\n\t\t\t// reject at blob-build time. Timeline nodes are not on this graph: they spawn later, through\n\t\t\t// the request queue, so a loop among them is a runaway rather than a stack overflow.\n\t\t\t// Keyed entries only: a scene composition is never referenced, so it can't be on a cycle.\n\t\t\tvar state = new Dictionary<EntityDefKey, byte>(_byKey.Count);\n\t\t\tforeach (var kvp in _byKey)\n\t\t\t\tDetectSpawnCycle(kvp.Key, state);\n\t\t}\n\n\t\t// DFS colouring: revisiting an in-stack (grey = 1) node is a back edge ⇒ cycle; 2 = done.\n\t\tvoid DetectSpawnCycle(EntityDefKey key, Dictionary<EntityDefKey, byte> state)\n\t\t{\n\t\t\tvar entry = _entries[_byKey[key]];\n\t\t\tstate.TryGetValue(key, out var mark);\n\t\t\tAssert.IsTrue(mark != 1, $\"definition spawn cycle through '{entry.Name}'\");\n\t\t\tif (mark == 2)\n\t\t\t\treturn;\n\n\t\t\tstate[key] = 1;\n\t\t\tforeach (var nested in entry.Nested)\n\t\t\t{\n\t\t\t\tvar n = nested.DefinitionAssetRef?.Definition;\n\t\t\t\tif (n != null && _byKey.ContainsKey(n.Id))\n\t\t\t\t\tDetectSpawnCycle(n.Id, state);\n\t\t\t}\n\t\t\tforeach (var config in entry.Modules)\n\t\t\t{\n\t\t\t\tif (config is not AbilityOwnerModuleConfig owner)\n\t\t\t\t\tcontinue;\n\t\t\t\tfor (int list = 0; list < 2; list++)\n\t\t\t\t\tforeach (var asset in list == 0 ? owner.Ordered : owner.Auto)\n\t\t\t\t\t{\n\t\t\t\t\t\tvar a = asset?.Definition;\n\t\t\t\t\t\tif (a != null && _byKey.ContainsKey(a.Id))\n\t\t\t\t\t\t\tDetectSpawnCycle(a.Id, state);\n\t\t\t\t\t}\n\t\t\t}\n\t\t\tstate[key] = 2;\n\t\t}\n\n\t\t// Needs a spawn placement: the Transform module brings CPosition/CRotation. Implies, not Has —\n\t\t// Body pulls it in through the closure, and the editor validations read the authored set.\n\t\tinternal static bool HasTransform(IReadOnlyList<ModuleConfig> modules)\n\t\t\t=> ModuleClosure.Implies(modules, EntityModules.Transform);\n// … (truncated)"
     }
    },
    {
     "line": 313,
     "state": {
      "comment": "// One spawn recurses into nested defs and abilities, so a cycle there would recurse forever —\n// reject at blob-build time. Timeline nodes are not on this graph: they spawn later, through\n// the request queue, so a loop among them is a runaway rather than a stack overflow.\n// Keyed entries only: a scene composition is never referenced, so it can't be on a cycle.",
      "annotated_code": "\t\t\tvar state = new Dictionary<EntityDefKey, byte>(_byKey.Count);\n\t\t\tforeach (var kvp in _byKey)\n\t\t\t\tDetectSpawnCycle(kvp.Key, state);"
     }
    },
    {
     "line": 322,
     "state": {
      "comment": "// DFS colouring: revisiting an in-stack (grey = 1) node is a back edge ⇒ cycle; 2 = done.",
      "annotated_code": "\t\tvoid DetectSpawnCycle(EntityDefKey key, Dictionary<EntityDefKey, byte> state)\n\t\t{\n\t\t\tvar entry = _entries[_byKey[key]];\n\t\t\tstate.TryGetValue(key, out var mark);\n\t\t\tAssert.IsTrue(mark != 1, $\"definition spawn cycle through '{entry.Name}'\");\n\t\t\tif (mark == 2)\n\t\t\t\treturn;\n\n\t\t\tstate[key] = 1;\n\t\t\tforeach (var nested in entry.Nested)\n\t\t\t{\n\t\t\t\tvar n = nested.DefinitionAssetRef?.Definition;\n\t\t\t\tif (n != null && _byKey.ContainsKey(n.Id))\n\t\t\t\t\tDetectSpawnCycle(n.Id, state);\n\t\t\t}\n\t\t\tforeach (var config in entry.Modules)\n\t\t\t{\n\t\t\t\tif (config is not AbilityOwnerModuleConfig owner)\n\t\t\t\t\tcontinue;\n\t\t\t\tfor (int list = 0; list < 2; list++)\n\t\t\t\t\tforeach (var asset in list == 0 ? owner.Ordered : owner.Auto)\n\t\t\t\t\t{\n\t\t\t\t\t\tvar a = asset?.Definition;\n\t\t\t\t\t\tif (a != null && _byKey.ContainsKey(a.Id))\n\t\t\t\t\t\t\tDetectSpawnCycle(a.Id, state);\n\t\t\t\t\t}\n\t\t\t}\n\t\t\tstate[key] = 2;\n\t\t}\n\n\t\t// Needs a spawn placement: the Transform module brings CPosition/CRotation. Implies, not Has —\n\t\t// Body pulls it in through the closure, and the editor validations read the authored set.\n\t\tinternal static bool HasTransform(IReadOnlyList<ModuleConfig> modules)\n\t\t\t=> ModuleClosure.Implies(modules, EntityModules.Transform);\n\n\t\t// Proxy for \"can hold attributes/resources\": the Stats module is what brings them.\n\t\tinternal static bool HasStats(IReadOnlyList<ModuleConfig> modules)\n\t\t\t=> Has(modules, EntityModules.Stats);\n\n\t\tinternal static bool Has(IReadOnlyList<ModuleConfig> modules, EntityModule module)\n\t\t{\n\t\t\tif (modules == null)\n\t\t\t\treturn false;\n\t\t\tforeach (var config in modules)\n\t\t\t\tif (config != null && config.Module == module)\n\t\t\t\t\treturn true;\n\t\t\treturn false;\n\t\t}"
     }
    },
    {
     "line": 353,
     "state": {
      "comment": "// Needs a spawn placement: the Transform module brings CPosition/CRotation. Implies, not Has —\n// Body pulls it in through the closure, and the editor validations read the authored set.",
      "annotated_code": "\t\tinternal static bool HasTransform(IReadOnlyList<ModuleConfig> modules)\n\t\t\t=> ModuleClosure.Implies(modules, EntityModules.Transform);\n\n\t\t// Proxy for \"can hold attributes/resources\": the Stats module is what brings them.\n\t\tinternal static bool HasStats(IReadOnlyList<ModuleConfig> modules)\n\t\t\t=> Has(modules, EntityModules.Stats);\n\n\t\tinternal static bool Has(IReadOnlyList<ModuleConfig> modules, EntityModule module)\n\t\t{\n\t\t\tif (modules == null)\n\t\t\t\treturn false;\n\t\t\tforeach (var config in modules)\n\t\t\t\tif (config != null && config.Module == module)\n\t\t\t\t\treturn true;\n\t\t\treturn false;\n\t\t}"
     }
    },
    {
     "line": 358,
     "state": {
      "comment": "// Proxy for \"can hold attributes/resources\": the Stats module is what brings them.",
      "annotated_code": "\t\tinternal static bool HasStats(IReadOnlyList<ModuleConfig> modules)\n\t\t\t=> Has(modules, EntityModules.Stats);\n\n\t\tinternal static bool Has(IReadOnlyList<ModuleConfig> modules, EntityModule module)\n\t\t{\n\t\t\tif (modules == null)\n\t\t\t\treturn false;\n\t\t\tforeach (var config in modules)\n\t\t\t\tif (config != null && config.Module == module)\n\t\t\t\t\treturn true;\n\t\t\treturn false;\n\t\t}"
     }
    }
   ],
   "declarations": [
    {
     "line": 38,
     "head": "static readonly IReadOnlyList<NestedDefinition> NoNested = Array.Empty<NestedDefinition>();",
     "state": {
      "declaration": "\t\tstatic readonly IReadOnlyList<NestedDefinition> NoNested = Array.Empty<NestedDefinition>();",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 40,
     "head": "readonly TemplateRegistry _registry = new();",
     "state": {
      "declaration": "\t\treadonly TemplateRegistry _registry = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 44,
     "head": "readonly List<Entry> _entries = new();",
     "state": {
      "declaration": "\t\treadonly List<Entry> _entries = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Every composition declared this run, keyed or not — scene compositions bake transient, so they\n// have no key but still carry a nested list to validate.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 48,
     "head": "readonly Dictionary<EntityDefKey, int> _byKey = new();",
     "state": {
      "declaration": "\t\treadonly Dictionary<EntityDefKey, int> _byKey = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// The keyed subset, by index into _entries: what BuildBlobRegistry publishes and what a nested\n// or spawn reference resolves through.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 53,
     "head": "readonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();",
     "state": {
      "declaration": "\t\treadonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Timeline node config -> its generated key. Dedups a shared composition declared more than\n// once (e.g. one definition asset used by several scene initializers) to one node def. Per-run\n// (new compiler each world), so it never resolves to a stale key from a prior run.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 56,
     "head": "readonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();",
     "state": {
      "declaration": "\t\treadonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Inline spawn definition -> its generated key. Same per-run lifetime as _nodeKeys.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 60,
     "head": "readonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();",
     "state": {
      "declaration": "\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Definition asset -> its key, for assets already declared. Also the re-entry guard: a reference\n// cycle resolves to the key instead of recursing.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 62,
     "head": "int _generatedCounter;",
     "state": {
      "declaration": "\t\tint _generatedCounter;",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 63,
     "head": "int _timelineCount;",
     "state": {
      "declaration": "\t\tint _timelineCount;",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 68,
     "head": "internal void Declare(Initializer[] sceneInitializers, EntityDefinitionCollection collection = null)",
     "state": {
      "declaration": "\t\tinternal void Declare(Initializer[] sceneInitializers, EntityDefinitionCollection collection = null)\n\t\t{\n\t\t\tforeach (var initializer in sceneInitializers)\n\t\t\t\tif (initializer is EntityInitializer entityInitializer)\n\t\t\t\t\tentityInitializer.DeclareTemplate(this);\n\n\t\t\tDeclareDefinitions(collection);\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Declare phase — before world construction. Scene compositions and every collection definition\n// register their archetypes (a definition gets a template whether or not a scene references it,\n// so any spawn-by-key has a group to resolve).",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 78,
     "head": "public IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();",
     "state": {
      "declaration": "\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Feeds the registered archetypes to the WorldBuilder; call once after Declare.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 81,
     "head": "internal TemplateRegistry.RegisteredArchetype RegisterComposition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)",
     "state": {
      "declaration": "\t\tinternal TemplateRegistry.RegisteredArchetype RegisterComposition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = RegisterEntry(nameHint, configs, nested);\n\t\t\treturn _entries[entry].Archetype;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Keyless entry point, for a scene composition that bakes transient.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 88,
     "head": "void DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)",
     "state": {
      "declaration": "\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = RegisterEntry(nameHint, configs, nested);",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Keyed entry point: same registration, plus the key -> entry lookup spawn resolves through.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 100,
     "head": "int RegisterEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)",
     "state": {
      "declaration": "\t\tint RegisterEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimelineNodes(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = DeclareEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _entries.Count;\n\t\t\t_entries.Add(new Entry(nameHint, configs, nested ?? NoNested, archetype));\n\t\t\tDeclareNested(nested);\n\t\t\treturn index;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Every composition funnels through here — one place that walks the modules, records the entry\n// and declares the nested defs, so no declare site can register a composition and forget half of\n// it. AbilityOwner and ActionTimeline carry references to other definitions — declare each\n// (minting a generated key for inline ones) and stamp the key back onto the config for Bake to\n// read. Augments configs in place, so the caller's list is the effective composition — the same\n// one that must reach Bake, or the template carries components the blob has no payload for.\n// Returns the entry index: the module walk and the nested declare both append entries of their\n// own, so neither end of the list identifies this one. The caller hands over configs — the entry\n// keeps it, so it must not be pooled or reused.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 133,
     "head": "void DeclareAbilities(AbilityOwnerModuleConfig owner)",
     "state": {
      "declaration": "\t\tvoid DeclareAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = Declare(owner.Ordered);\n\t\t\towner.ResolvedAuto = Declare(owner.Auto);\n\n\t\t\tEntityDefKey[] Declare(EntityDefinitionAsset[] list)\n\t\t\t{\n\t\t\t\tvar keys = new EntityDefKey[list.Length];\n\t\t\t\tfor (int i = 0; i < list.Length; i++)\n\t\t\t\t\tif (list[i] != null)\n\t\t\t\t\t\tkeys[i] = DeclareDefinitionAsset(list[i]);\n\t\t\t\treturn keys;\n\t\t\t}\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// An ability is an ordinary definition asset (the Ability contract is what marks it castable), so\n// it declares through the shared path and shares the asset's own key with every other reference\n// to it.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 152,
     "head": "EntityDefKey DeclareEntityCreate(EntityCreateConfig config, string context)",
     "state": {
      "declaration": "\t\tEntityDefKey DeclareEntityCreate(EntityCreateConfig config, string context)\n\t\t{\n\t\t\tif (config.DefinitionAsset != null)\n\t\t\t\treturn DeclareDefinitionAsset(config.DefinitionAsset);\n\n\t\t\tif (_inlineCreateKeys.TryGetValue(config, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = config.InlineDefinition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\t\t\tif (def.Modules == null || def.Modules.Count == 0)\n\t\t\t\treturn default; // Validate reports it; nothing to declare\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_inlineCreateKeys[config] = key; // set before recursion — guards a definition spawning itself\n\t\t\tDeclareKeyed(key, context, configs, def.Nested);\n\t\t\treturn key;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// An external ref reuses the asset's own key: unlike a timeline node there is no per-site override,\n// so minting a fresh key would duplicate the archetype and its blob for nothing. Inline\n// definitions get a generated key, deduped by config identity so one config declares once —\n// the first declare site's path names it, since one shared config has no single owner.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 175,
     "head": "internal EntityDefKey DeclareDefinitionAsset(EntityDefinitionAsset asset)",
     "state": {
      "declaration": "\t\tinternal EntityDefKey DeclareDefinitionAsset(EntityDefinitionAsset asset)\n\t\t{\n\t\t\tAssert.IsNotNull(asset, \"declare definition: null asset\");\n\t\t\tif (_assetKeys.TryGetValue(asset, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar definition = asset.Definition;\n\t\t\tAssert.IsNotNull(definition, $\"'{asset.name}': empty definition\");\n\t\t\tAssert.IsFalse(definition.Id.IsNone, $\"'{asset.name}': definition without id\");\n\t\t\tAssert.IsFalse(definition.Id.IsGenerated, $\"'{asset.name}': asset id in the compiler-minted range\");\n\t\t\tAssert.IsFalse(_byKey.ContainsKey(definition.Id), $\"'{asset.name}': duplicate definition id {definition.Id}\");\n\n\t\t\t_assetKeys[asset] = definition.Id; // set before recursion — guards a definition reaching itself\n\n\t\t\t// Copy: the declare augments the list, and the asset's own is serialized data.\n\t\t\tvar configs = new List<ModuleConfig>(definition.Modules);\n\t\t\tDeclareKeyed(definition.Id, asset.name, configs, definition.Nested);\n\t\t\treturn definition.Id;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// A referenced definition asset declares itself, so reachability is the membership rule — nothing\n// has to be listed anywhere for a reference to resolve. The collection only adds the roots\n// nothing references statically (spawn-by-key from code or a save).",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 197,
     "head": "void DeclareNested(IReadOnlyList<NestedDefinition> nested)",
     "state": {
      "declaration": "\t\tvoid DeclareNested(IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tif (nested == null)\n\t\t\t\treturn;\n\t\t\tforeach (var n in nested)\n\t\t\t\tif (n.DefinitionAssetRef != null)\n\t\t\t\t\tDeclareDefinitionAsset(n.DefinitionAssetRef);\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Nested defs spawn by key, so each must be declared — including from a scene initializer, whose\n// transient blob bakes its definition's nested list without declaring the definition itself.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 206,
     "head": "void DeclareTimelineNodes(ActionTimelineModuleConfig timeline, string context)",
     "state": {
      "declaration": "\t\tvoid DeclareTimelineNodes(ActionTimelineModuleConfig timeline, string context)\n\t\t{\n\t\t\t_timelineCount++;\n\t\t\tfor (int i = 0; i < timeline.Nodes.Count; i++)\n\t\t\t{\n\t\t\t\tvar node = timeline.Nodes[i];\n\t\t\t\tAssert.IsNotNull(node, $\"'{context}': empty node entry\");\n\t\t\t\tnode.ResolvedDefinition = DeclareTimelineNode(node, $\"{context}[{i}]\");\n\t\t\t}\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 222,
     "head": "EntityDefKey DeclareTimelineNode(TimelineNodeConfig node, string context)",
     "state": {
      "declaration": "\t\tEntityDefKey DeclareTimelineNode(TimelineNodeConfig node, string context)\n\t\t{\n\t\t\tif (_nodeKeys.TryGetValue(node, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = node.Definition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tModuleClosure.Ensure(configs, EntityModules.ConditionTime, () => new ConditionTimeModuleConfig());\n\n\t\t\t// A node is spawned with no placement (ActionTimelineSystem has no anchor to offer), so a body\n\t\t\t// would fail its transform check on every cast. The node's own actions place what they spawn.\n\t\t\tAssert.IsFalse(HasTransform(configs), $\"'{context}': timeline node is placed, but a node is spawned without a placement\");\n\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_nodeKeys[node] = key; // set before recursion — guards re-entry\n\t\t\tDeclareKeyed(key, node.DefinitionAsset != null ? $\"{context} ▸ {node.DefinitionAsset.name}\" : context, configs, def.Nested);\n\t\t\treturn key;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// A node's def is an inline definition wrapped as a time-gated reaction: its modules plus an\n// ensured ConditionTime (the framework's time gate; ActionTimelineSystem overrides the window\n// per cast — the reaction core follows from it via ModuleClosure). Always a fresh generated key\n// — even an external ref is copied here so the shared collection def keeps its ungated shape\n// (per-node overrides come later).",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 243,
     "head": "void DeclareDefinitions(EntityDefinitionCollection collection)",
     "state": {
      "declaration": "\t\tvoid DeclareDefinitions(EntityDefinitionCollection collection)\n\t\t{\n\t\t\tif (collection == null)\n\t\t\t\treturn;\n\n\t\t\tforeach (var asset in collection.Definitions)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(asset, $\"'{collection.name}': empty definition entry\");\n\t\t\t\tDeclareDefinitionAsset(asset);\n\t\t\t}\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 255,
     "head": "EntityDefKey MintGeneratedKey() => EntityDefKey.Generated(_generatedCounter++);",
     "state": {
      "declaration": "\t\tEntityDefKey MintGeneratedKey() => EntityDefKey.Generated(_generatedCounter++);",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 258,
     "head": "public IReadOnlyList<LookSource> BuildLooks()",
     "state": {
      "declaration": "\t\tpublic IReadOnlyList<LookSource> BuildLooks()\n\t\t{\n\t\t\tvar walked = new Dictionary<Look, LookSource>();\n\t\t\tvar looks = new LookSource[_entries.Count];\n\t\t\tfor (int i = 0; i < _entries.Count; i++)\n\t\t\t\tforeach (var config in _entries[i].Modules)\n\t\t\t\t{\n\t\t\t\t\tif (config is not LookModuleConfig lookConfig || lookConfig.Look == null)\n\t\t\t\t\t\tcontinue;\n\n\t\t\t\t\tif (!walked.TryGetValue(lookConfig.Look, out var source))\n\t\t\t\t\t\twalked.Add(lookConfig.Look, source = lookConfig.Look.Walk());\n\t\t\t\t\tlooks[i] = source;\n\t\t\t\t}\n\n\t\t\treturn looks;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// By entry, null for none. A look shared by several definitions is walked once.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 281,
     "head": "public void BuildBlobRegistry(DefinitionRegistry definitions)",
     "state": {
      "declaration": "\t\tpublic void BuildBlobRegistry(DefinitionRegistry definitions)\n\t\t{\n\t\t\tValidateNested();\n\t\t\tdefinitions.CreateDefinitionRegistry(_byKey.Count, _timelineCount);\n\t\t\tforeach (var kvp in _byKey)\n\t\t\t{\n\t\t\t\tvar entry = _entries[kvp.Value];\n\t\t\t\tdefinitions.AddEntityDefinition(kvp.Key, kvp.Value, entry.Name, entry.Modules, entry.Nested, entry.Archetype.TagSet);\n\t\t\t}\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Publishes every declared definition (collection + generated inline) into the\n// CDefinitionRegistry global, reusing the Declare archetypes. Each ActionTimeline module bakes\n// its TimelineBlob as a side-effect (into _timelines). The global's create/add/dispose live on\n// DefinitionRegistry (owns the accessor + world-lifetime teardown); this only drives the\n// iteration over the keys it holds. Runs at init after the world is built.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 297,
     "head": "void ValidateNested()",
     "state": {
      "declaration": "\t\tvoid ValidateNested()\n\t\t{\n\t\t\tforeach (var entry in _entries)\n\t\t\t{\n\t\t\t\tbool parentIsPlaced = HasTransform(entry.Modules);\n\t\t\t\tforeach (var n in entry.Nested)\n\t\t\t\t{\n\t\t\t\t\tvar nd = n.DefinitionAssetRef;\n\t\t\t\t\tif (nd == null)\n\t\t\t\t\t\tcontinue;\n\t\t\t\t\tvar def = nd.Definition;\n\t\t\t\t\tAssert.IsTrue(_byKey.ContainsKey(def.Id), $\"'{entry.Name}': nested '{def.Name}' was not declared\");\n\t\t\t\t\tAssert.IsTrue(parentIsPlaced || !HasTransform(def.Modules), $\"'{entry.Name}': nests placed '{def.Name}' but has no transform to anchor it\");\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// One spawn recurses into nested defs and abilities, so a cycle there would recurse forever —\n\t\t\t// reject at blob-build time. Timeline nodes are not on this graph: they spawn later, through\n\t\t\t// the request queue, so a loop among them is a runaway rather than a stack overflow.\n\t\t\t// Keyed entries only: a scene composition is never referenced, so it can't be on a cycle.\n\t\t\tvar state = new Dictionary<EntityDefKey, byte>(_byKey.Count);\n\t\t\tforeach (var kvp in _byKey)\n\t\t\t\tDetectSpawnCycle(kvp.Key, state);\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Two checks over the declared graph. A nested def that needs a transform requires the parent to\n// have one too — the nested def inherits the parent transform, spawn has nothing else to anchor it\n// (transform-as-override is deferred). Over every entry, not just the keyed ones: a scene\n// composition has no key but nests by key like any other parent.\n// Then the spawn-recursion cycle check below.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 323,
     "head": "void DetectSpawnCycle(EntityDefKey key, Dictionary<EntityDefKey, byte> state)",
     "state": {
      "declaration": "\t\tvoid DetectSpawnCycle(EntityDefKey key, Dictionary<EntityDefKey, byte> state)\n\t\t{\n\t\t\tvar entry = _entries[_byKey[key]];\n\t\t\tstate.TryGetValue(key, out var mark);\n\t\t\tAssert.IsTrue(mark != 1, $\"definition spawn cycle through '{entry.Name}'\");\n\t\t\tif (mark == 2)\n\t\t\t\treturn;\n\n\t\t\tstate[key] = 1;\n\t\t\tforeach (var nested in entry.Nested)\n\t\t\t{\n\t\t\t\tvar n = nested.DefinitionAssetRef?.Definition;\n\t\t\t\tif (n != null && _byKey.ContainsKey(n.Id))\n\t\t\t\t\tDetectSpawnCycle(n.Id, state);\n\t\t\t}\n\t\t\tforeach (var config in entry.Modules)\n\t\t\t{\n\t\t\t\tif (config is not AbilityOwnerModuleConfig owner)\n\t\t\t\t\tcontinue;\n\t\t\t\tfor (int list = 0; list < 2; list++)\n\t\t\t\t\tforeach (var asset in list == 0 ? owner.Ordered : owner.Auto)\n\t\t\t\t\t{\n\t\t\t\t\t\tvar a = asset?.Definition;\n\t\t\t\t\t\tif (a != null && _byKey.ContainsKey(a.Id))\n\t\t\t\t\t\t\tDetectSpawnCycle(a.Id, state);\n\t\t\t\t\t}\n\t\t\t}\n\t\t\tstate[key] = 2;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// DFS colouring: revisiting an in-stack (grey = 1) node is a back edge ⇒ cycle; 2 = done.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 355,
     "head": "internal static bool HasTransform(IReadOnlyList<ModuleConfig> modules)",
     "state": {
      "declaration": "\t\tinternal static bool HasTransform(IReadOnlyList<ModuleConfig> modules)\n\t\t\t=> ModuleClosure.Implies(modules, EntityModules.Transform);",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Needs a spawn placement: the Transform module brings CPosition/CRotation. Implies, not Has —\n// Body pulls it in through the closure, and the editor validations read the authored set.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 359,
     "head": "internal static bool HasStats(IReadOnlyList<ModuleConfig> modules)",
     "state": {
      "declaration": "\t\tinternal static bool HasStats(IReadOnlyList<ModuleConfig> modules)\n\t\t\t=> Has(modules, EntityModules.Stats);",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Proxy for \"can hold attributes/resources\": the Stats module is what brings them.",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 362,
     "head": "internal static bool Has(IReadOnlyList<ModuleConfig> modules, EntityModule module)",
     "state": {
      "declaration": "\t\tinternal static bool Has(IReadOnlyList<ModuleConfig> modules, EntityModule module)\n\t\t{\n\t\t\tif (modules == null)\n\t\t\t\treturn false;\n\t\t\tforeach (var config in modules)\n\t\t\t\tif (config != null && config.Module == module)\n\t\t\t\t\treturn true;\n\t\t\treturn false;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_entries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "Declare",
       "BuildTemplates",
       "RegisterComposition",
       "DeclareKeyed",
       "RegisterEntry",
       "DeclareAbilities",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimelineNodes",
       "DeclareTimelineNode",
       "DeclareDefinitions",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle",
       "HasTransform",
       "HasStats",
       "Has"
      ]
     }
    },
    {
     "line": 24,
     "head": "public readonly string Name;",
     "state": {
      "declaration": "\t\t\tpublic readonly string Name;",
      "type_signature": "readonly struct Entry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "Entry"
      ]
     }
    },
    {
     "line": 25,
     "head": "public readonly IReadOnlyList<ModuleConfig> Modules;",
     "state": {
      "declaration": "\t\t\tpublic readonly IReadOnlyList<ModuleConfig> Modules;",
      "type_signature": "readonly struct Entry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "Entry"
      ]
     }
    },
    {
     "line": 26,
     "head": "public readonly IReadOnlyList<NestedDefinition> Nested;",
     "state": {
      "declaration": "\t\t\tpublic readonly IReadOnlyList<NestedDefinition> Nested;",
      "type_signature": "readonly struct Entry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "Entry"
      ]
     }
    },
    {
     "line": 27,
     "head": "public readonly TemplateRegistry.RegisteredArchetype Archetype;",
     "state": {
      "declaration": "\t\t\tpublic readonly TemplateRegistry.RegisteredArchetype Archetype;",
      "type_signature": "readonly struct Entry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "Entry"
      ]
     }
    },
    {
     "line": 29,
     "head": "public Entry(string name, IReadOnlyList<ModuleConfig> modules, IReadOnlyList<NestedDefinition> nested, TemplateRegistry.RegisteredArchetype archetype)",
     "state": {
      "declaration": "\t\t\tpublic Entry(string name, IReadOnlyList<ModuleConfig> modules, IReadOnlyList<NestedDefinition> nested, TemplateRegistry.RegisteredArchetype archetype)\n\t\t\t{\n\t\t\t\tName = name;\n\t\t\t\tModules = modules;\n\t\t\t\tNested = nested;\n\t\t\t\tArchetype = archetype;\n\t\t\t}",
      "type_signature": "readonly struct Entry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "Entry"
      ]
     }
    }
   ],
   "types": [
    {
     "line": 20,
     "head": "public sealed class DefinitionCompiler",
     "state": {
      "signatures": [
       "internal void Declare(Initializer[] sceneInitializers, EntityDefinitionCollection collection = null)",
       "public IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();",
       "internal TemplateRegistry.RegisteredArchetype RegisterComposition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)",
       "void DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)",
       "int RegisterEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)",
       "void DeclareAbilities(AbilityOwnerModuleConfig owner)",
       "EntityDefKey DeclareEntityCreate(EntityCreateConfig config, string context)",
       "internal EntityDefKey DeclareDefinitionAsset(EntityDefinitionAsset asset)",
       "void DeclareNested(IReadOnlyList<NestedDefinition> nested)",
       "void DeclareTimelineNodes(ActionTimelineModuleConfig timeline, string context)",
       "EntityDefKey DeclareTimelineNode(TimelineNodeConfig node, string context)",
       "void DeclareDefinitions(EntityDefinitionCollection collection)",
       "EntityDefKey MintGeneratedKey() => EntityDefKey.Generated(_generatedCounter++);",
       "public IReadOnlyList<LookSource> BuildLooks()",
       "public void BuildBlobRegistry(DefinitionRegistry definitions)",
       "void ValidateNested()",
       "void DetectSpawnCycle(EntityDefKey key, Dictionary<EntityDefKey, byte> state)",
       "internal static bool HasTransform(IReadOnlyList<ModuleConfig> modules)",
       "internal static bool HasStats(IReadOnlyList<ModuleConfig> modules)",
       "internal static bool Has(IReadOnlyList<ModuleConfig> modules, EntityModule module)"
      ]
     }
    },
    {
     "line": 22,
     "head": "readonly struct Entry",
     "state": {
      "signatures": [
       "public Entry(string name, IReadOnlyList<ModuleConfig> modules, IReadOnlyList<NestedDefinition> nested, TemplateRegistry.RegisteredArchetype archetype)"
      ]
     }
    }
   ]
  },
  "v1": {
   "comment_blocks": [
    {
     "line": 8,
     "state": {
      "comment": "// Authored compositions -> runtime archetypes + definition blobs. Phases, in order:\n// 1. DeclareUnkeyed / DeclareCollection — before world build: registers archetypes for every reachable definition.\n// 2. BuildTemplates — before world build.\n// 3. BuildBlobRegistry — after world build: publishes keyed definitions into DefinitionRegistry.",
      "annotated_code": "\tpublic sealed class DefinitionCompiler\n\t{\n\t\treadonly struct DefinitionEntry\n\t\t{\n\t\t\tpublic readonly string Name;\n\t\t\tpublic readonly IReadOnlyList<ModuleConfig> Modules;\n\t\t\tpublic readonly IReadOnlyList<NestedDefinition> Nested;\n\t\t\tpublic readonly TemplateRegistry.RegisteredArchetype Archetype;\n\n\t\t\tpublic DefinitionEntry(string name, IReadOnlyList<ModuleConfig> modules, IReadOnlyList<NestedDefinition> nested, TemplateRegistry.RegisteredArchetype archetype)\n\t\t\t{\n\t\t\t\tName = name;\n\t\t\t\tModules = modules;\n\t\t\t\tNested = nested;\n\t\t\t\tArchetype = archetype;\n\t\t\t}\n\t\t}\n\n\t\tstatic readonly IReadOnlyList<NestedDefinition> NoNested = Array.Empty<NestedDefinition>();\n\n\t\treadonly TemplateRegistry _registry = new();\n\n\t\treadonly List<DefinitionEntry> _allDefinitionEntries = new();\n\n\t\t// Published subset.\n\t\treadonly Dictionary<EntityDefKey, int> _byKey = new();\n\n\t\t// Dedups timeline nodes.\n\t\treadonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();\n\n\t\treadonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();\n\n\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();\n\n\t\tint _generatedCounter;\n\t\tint _timelineCount;\n\n\t\t// Phase 1, before world build.\n\t\tinternal void DeclareCollection(EntityDefinitionCollection collection)\n\t\t{\n\t\t\tAssert.IsNotNull(collection, \"DeclareCollection: null collection\");\n\t\t\tforeach (var asset in collection.Definitions)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(asset, $\"'{collection.name}': empty definition entry\");\n\t\t\t\tDeclareDefinitionAsset(asset);\n\t\t\t}\n\t\t}\n\n\t\t// Phase 1, before world build.\n\t\tinternal TemplateRegistry.RegisteredArchetype DeclareUnkeyed(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = DeclareEntry(nameHint, configs, nested);\n\t\t\treturn _allDefinitionEntries[entry].Archetype;\n\t\t}\n\n\t\t// Phase 2.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = DeclareEntry(nameHint, configs, nested);\n// … (truncated)"
     }
    },
    {
     "line": 36,
     "state": {
      "comment": "// Published subset.",
      "annotated_code": "\t\treadonly Dictionary<EntityDefKey, int> _byKey = new();\n\n\t\t// Dedups timeline nodes.\n\t\treadonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();\n\n\t\treadonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();\n\n\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();\n\n\t\tint _generatedCounter;\n\t\tint _timelineCount;\n\n\t\t// Phase 1, before world build.\n\t\tinternal void DeclareCollection(EntityDefinitionCollection collection)\n\t\t{\n\t\t\tAssert.IsNotNull(collection, \"DeclareCollection: null collection\");\n\t\t\tforeach (var asset in collection.Definitions)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(asset, $\"'{collection.name}': empty definition entry\");\n\t\t\t\tDeclareDefinitionAsset(asset);\n\t\t\t}\n\t\t}\n\n\t\t// Phase 1, before world build.\n\t\tinternal TemplateRegistry.RegisteredArchetype DeclareUnkeyed(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = DeclareEntry(nameHint, configs, nested);\n\t\t\treturn _allDefinitionEntries[entry].Archetype;\n\t\t}\n\n\t\t// Phase 2.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = DeclareEntry(nameHint, configs, nested);\n\n\t\t// Takes ownership of configs and the configs in it: augments the list, modifies the configs.\n\t\t// Returns entry index: module walk (before) and nested declare (after) append their own entries.\n\t\tint DeclareEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimeline(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = DeclareEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n// … (truncated)"
     }
    },
    {
     "line": 39,
     "state": {
      "comment": "// Dedups timeline nodes.",
      "annotated_code": "\t\treadonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();\n\n\t\treadonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();\n\n\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();\n\n\t\tint _generatedCounter;\n\t\tint _timelineCount;\n\n\t\t// Phase 1, before world build.\n\t\tinternal void DeclareCollection(EntityDefinitionCollection collection)\n\t\t{\n\t\t\tAssert.IsNotNull(collection, \"DeclareCollection: null collection\");\n\t\t\tforeach (var asset in collection.Definitions)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(asset, $\"'{collection.name}': empty definition entry\");\n\t\t\t\tDeclareDefinitionAsset(asset);\n\t\t\t}\n\t\t}\n\n\t\t// Phase 1, before world build.\n\t\tinternal TemplateRegistry.RegisteredArchetype DeclareUnkeyed(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = DeclareEntry(nameHint, configs, nested);\n\t\t\treturn _allDefinitionEntries[entry].Archetype;\n\t\t}\n\n\t\t// Phase 2.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = DeclareEntry(nameHint, configs, nested);\n\n\t\t// Takes ownership of configs and the configs in it: augments the list, modifies the configs.\n\t\t// Returns entry index: module walk (before) and nested declare (after) append their own entries.\n\t\tint DeclareEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimeline(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = DeclareEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _allDefinitionEntries.Count;\n// … (truncated)"
     }
    },
    {
     "line": 49,
     "state": {
      "comment": "// Phase 1, before world build.",
      "annotated_code": "\t\tinternal void DeclareCollection(EntityDefinitionCollection collection)\n\t\t{\n\t\t\tAssert.IsNotNull(collection, \"DeclareCollection: null collection\");\n\t\t\tforeach (var asset in collection.Definitions)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(asset, $\"'{collection.name}': empty definition entry\");\n\t\t\t\tDeclareDefinitionAsset(asset);\n\t\t\t}\n\t\t}\n\n\t\t// Phase 1, before world build.\n\t\tinternal TemplateRegistry.RegisteredArchetype DeclareUnkeyed(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = DeclareEntry(nameHint, configs, nested);\n\t\t\treturn _allDefinitionEntries[entry].Archetype;\n\t\t}\n\n\t\t// Phase 2.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = DeclareEntry(nameHint, configs, nested);\n\n\t\t// Takes ownership of configs and the configs in it: augments the list, modifies the configs.\n\t\t// Returns entry index: module walk (before) and nested declare (after) append their own entries.\n\t\tint DeclareEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimeline(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = DeclareEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _allDefinitionEntries.Count;\n\t\t\t_allDefinitionEntries.Add(new DefinitionEntry(nameHint, configs, nested ?? NoNested, archetype));\n\t\t\tDeclareNested(nested);\n\t\t\treturn index;\n\t\t}\n\n\t\tvoid DeclareAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = DeclareDefinitionAssets(owner.Ordered);\n\t\t\towner.ResolvedAuto = DeclareDefinitionAssets(owner.Auto);\n\t\t}\n// … (truncated)"
     }
    },
    {
     "line": 60,
     "state": {
      "comment": "// Phase 1, before world build.",
      "annotated_code": "\t\tinternal TemplateRegistry.RegisteredArchetype DeclareUnkeyed(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = DeclareEntry(nameHint, configs, nested);\n\t\t\treturn _allDefinitionEntries[entry].Archetype;\n\t\t}\n\n\t\t// Phase 2.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = DeclareEntry(nameHint, configs, nested);\n\n\t\t// Takes ownership of configs and the configs in it: augments the list, modifies the configs.\n\t\t// Returns entry index: module walk (before) and nested declare (after) append their own entries.\n\t\tint DeclareEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimeline(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = DeclareEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _allDefinitionEntries.Count;\n\t\t\t_allDefinitionEntries.Add(new DefinitionEntry(nameHint, configs, nested ?? NoNested, archetype));\n\t\t\tDeclareNested(nested);\n\t\t\treturn index;\n\t\t}\n\n\t\tvoid DeclareAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = DeclareDefinitionAssets(owner.Ordered);\n\t\t\towner.ResolvedAuto = DeclareDefinitionAssets(owner.Auto);\n\t\t}\n\n\t\t// Null entries keep their slot with a default key.\n\t\tEntityDefKey[] DeclareDefinitionAssets(EntityDefinitionAsset[] assets)\n\t\t{\n\t\t\tvar keys = new EntityDefKey[assets.Length];\n\t\t\tfor (int i = 0; i < assets.Length; i++)\n\t\t\t\tif (assets[i] != null)\n\t\t\t\t\tkeys[i] = DeclareDefinitionAsset(assets[i]);\n\t\t\treturn keys;\n\t\t}\n\n// … (truncated)"
     }
    },
    {
     "line": 67,
     "state": {
      "comment": "// Phase 2.",
      "annotated_code": "\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = DeclareEntry(nameHint, configs, nested);\n\n\t\t// Takes ownership of configs and the configs in it: augments the list, modifies the configs.\n\t\t// Returns entry index: module walk (before) and nested declare (after) append their own entries.\n\t\tint DeclareEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimeline(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = DeclareEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _allDefinitionEntries.Count;\n\t\t\t_allDefinitionEntries.Add(new DefinitionEntry(nameHint, configs, nested ?? NoNested, archetype));\n\t\t\tDeclareNested(nested);\n\t\t\treturn index;\n\t\t}\n\n\t\tvoid DeclareAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = DeclareDefinitionAssets(owner.Ordered);\n\t\t\towner.ResolvedAuto = DeclareDefinitionAssets(owner.Auto);\n\t\t}\n\n\t\t// Null entries keep their slot with a default key.\n\t\tEntityDefKey[] DeclareDefinitionAssets(EntityDefinitionAsset[] assets)\n\t\t{\n\t\t\tvar keys = new EntityDefKey[assets.Length];\n\t\t\tfor (int i = 0; i < assets.Length; i++)\n\t\t\t\tif (assets[i] != null)\n\t\t\t\t\tkeys[i] = DeclareDefinitionAsset(assets[i]);\n\t\t\treturn keys;\n\t\t}\n\n\t\tEntityDefKey DeclareEntityCreate(EntityCreateConfig config, string context)\n\t\t{\n\t\t\t// Asset ref: reuses the asset's key.\n\t\t\tif (config.DefinitionAsset != null)\n\t\t\t\treturn DeclareDefinitionAsset(config.DefinitionAsset);\n\n\t\t\t// Inline: generated key, deduped by config identity.\n// … (truncated)"
     }
    },
    {
     "line": 73,
     "state": {
      "comment": "// Takes ownership of configs and the configs in it: augments the list, modifies the configs.\n// Returns entry index: module walk (before) and nested declare (after) append their own entries.",
      "annotated_code": "\t\tint DeclareEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimeline(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = DeclareEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _allDefinitionEntries.Count;\n\t\t\t_allDefinitionEntries.Add(new DefinitionEntry(nameHint, configs, nested ?? NoNested, archetype));\n\t\t\tDeclareNested(nested);\n\t\t\treturn index;\n\t\t}\n\n\t\tvoid DeclareAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = DeclareDefinitionAssets(owner.Ordered);\n\t\t\towner.ResolvedAuto = DeclareDefinitionAssets(owner.Auto);\n\t\t}\n\n\t\t// Null entries keep their slot with a default key.\n\t\tEntityDefKey[] DeclareDefinitionAssets(EntityDefinitionAsset[] assets)\n\t\t{\n\t\t\tvar keys = new EntityDefKey[assets.Length];\n\t\t\tfor (int i = 0; i < assets.Length; i++)\n\t\t\t\tif (assets[i] != null)\n\t\t\t\t\tkeys[i] = DeclareDefinitionAsset(assets[i]);\n\t\t\treturn keys;\n\t\t}\n\n\t\tEntityDefKey DeclareEntityCreate(EntityCreateConfig config, string context)\n\t\t{\n\t\t\t// Asset ref: reuses the asset's key.\n\t\t\tif (config.DefinitionAsset != null)\n\t\t\t\treturn DeclareDefinitionAsset(config.DefinitionAsset);\n\n\t\t\t// Inline: generated key, deduped by config identity.\n\t\t\tif (_inlineCreateKeys.TryGetValue(config, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = config.InlineDefinition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\t\t\tif (def.Modules == null || def.Modules.Count == 0)\n\t\t\t\treturn default; // Validate reports it; nothing to declare\n// … (truncated)"
     }
    },
    {
     "line": 111,
     "state": {
      "comment": "// Null entries keep their slot with a default key.",
      "annotated_code": "\t\tEntityDefKey[] DeclareDefinitionAssets(EntityDefinitionAsset[] assets)\n\t\t{\n\t\t\tvar keys = new EntityDefKey[assets.Length];\n\t\t\tfor (int i = 0; i < assets.Length; i++)\n\t\t\t\tif (assets[i] != null)\n\t\t\t\t\tkeys[i] = DeclareDefinitionAsset(assets[i]);\n\t\t\treturn keys;\n\t\t}\n\n\t\tEntityDefKey DeclareEntityCreate(EntityCreateConfig config, string context)\n\t\t{\n\t\t\t// Asset ref: reuses the asset's key.\n\t\t\tif (config.DefinitionAsset != null)\n\t\t\t\treturn DeclareDefinitionAsset(config.DefinitionAsset);\n\n\t\t\t// Inline: generated key, deduped by config identity.\n\t\t\tif (_inlineCreateKeys.TryGetValue(config, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = config.InlineDefinition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\t\t\tif (def.Modules == null || def.Modules.Count == 0)\n\t\t\t\treturn default; // Validate reports it; nothing to declare\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_inlineCreateKeys[config] = key; // set before recursion — guards a definition spawning itself\n\t\t\tDeclareKeyed(key, context, configs, def.Nested);\n\t\t\treturn key;\n\t\t}\n\n\t\tinternal EntityDefKey DeclareDefinitionAsset(EntityDefinitionAsset asset)\n\t\t{\n\t\t\tAssert.IsNotNull(asset, \"declare definition: null asset\");\n\t\t\tif (_assetKeys.TryGetValue(asset, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar definition = asset.Definition;\n\t\t\tAssert.IsNotNull(definition, $\"'{asset.name}': empty definition\");\n\t\t\tAssert.IsFalse(definition.Id.IsNone, $\"'{asset.name}': definition without id\");\n\t\t\tAssert.IsFalse(definition.Id.IsGenerated, $\"'{asset.name}': asset id in the compiler-minted range\");\n\t\t\tAssert.IsFalse(_byKey.ContainsKey(definition.Id), $\"'{asset.name}': duplicate definition id {definition.Id}\");\n\n\t\t\t_assetKeys[asset] = definition.Id; // set before recursion — guards a definition reaching itself\n\n\t\t\t// Copy: declare augments the list; the asset's serialized modules must stay untouched.\n\t\t\tvar configs = new List<ModuleConfig>(definition.Modules);\n\t\t\tDeclareKeyed(definition.Id, asset.name, configs, definition.Nested);\n\t\t\treturn definition.Id;\n\t\t}\n\n\t\tvoid DeclareNested(IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tif (nested == null)\n\t\t\t\treturn;\n\t\t\tforeach (var n in nested)\n\t\t\t\tif (n.DefinitionAssetRef != null)\n\t\t\t\t\tDeclareDefinitionAsset(n.DefinitionAssetRef);\n\t\t}\n\n// … (truncated)"
     }
    },
    {
     "line": 123,
     "state": {
      "comment": "// Asset ref: reuses the asset's key.",
      "annotated_code": "\t\t\tif (config.DefinitionAsset != null)\n\t\t\t\treturn DeclareDefinitionAsset(config.DefinitionAsset);\n\n\t\t\t// Inline: generated key, deduped by config identity.\n\t\t\tif (_inlineCreateKeys.TryGetValue(config, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = config.InlineDefinition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\t\t\tif (def.Modules == null || def.Modules.Count == 0)\n\t\t\t\treturn default; // Validate reports it; nothing to declare\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_inlineCreateKeys[config] = key; // set before recursion — guards a definition spawning itself\n\t\t\tDeclareKeyed(key, context, configs, def.Nested);\n\t\t\treturn key;"
     }
    },
    {
     "line": 127,
     "state": {
      "comment": "// Inline: generated key, deduped by config identity.",
      "annotated_code": "\t\t\tif (_inlineCreateKeys.TryGetValue(config, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = config.InlineDefinition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\t\t\tif (def.Modules == null || def.Modules.Count == 0)\n\t\t\t\treturn default; // Validate reports it; nothing to declare\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_inlineCreateKeys[config] = key; // set before recursion — guards a definition spawning itself\n\t\t\tDeclareKeyed(key, context, configs, def.Nested);\n\t\t\treturn key;"
     }
    },
    {
     "line": 157,
     "state": {
      "comment": "// Copy: declare augments the list; the asset's serialized modules must stay untouched.",
      "annotated_code": "\t\t\tvar configs = new List<ModuleConfig>(definition.Modules);\n\t\t\tDeclareKeyed(definition.Id, asset.name, configs, definition.Nested);\n\t\t\treturn definition.Id;"
     }
    },
    {
     "line": 204,
     "state": {
      "comment": "// By entry, null for none.",
      "annotated_code": "\t\tpublic IReadOnlyList<LookSource> BuildLooks()\n\t\t{\n\t\t\t// A look shared by several definitions is walked once.\n\t\t\tvar walked = new Dictionary<Look, LookSource>();\n\t\t\tvar looks = new LookSource[_allDefinitionEntries.Count];\n\t\t\tfor (int i = 0; i < _allDefinitionEntries.Count; i++)\n\t\t\t\tforeach (var config in _allDefinitionEntries[i].Modules)\n\t\t\t\t{\n\t\t\t\t\tif (config is not LookModuleConfig lookConfig || lookConfig.Look == null)\n\t\t\t\t\t\tcontinue;\n\n\t\t\t\t\tif (!walked.TryGetValue(lookConfig.Look, out var source))\n\t\t\t\t\t\twalked.Add(lookConfig.Look, source = lookConfig.Look.Walk());\n\t\t\t\t\tlooks[i] = source;\n\t\t\t\t}\n\n\t\t\treturn looks;\n\t\t}\n\n\t\t// Phase 3, after world build. Publishes every keyed entity definition into DefinitionRegistry.\n\t\tpublic void BuildBlobRegistry(DefinitionRegistry definitions)\n\t\t{\n\t\t\tValidateNested();\n\t\t\tdefinitions.CreateDefinitionRegistry(_byKey.Count, _timelineCount);\n\t\t\tforeach (var kvp in _byKey)\n\t\t\t{\n\t\t\t\tvar entry = _allDefinitionEntries[kvp.Value];\n\t\t\t\tdefinitions.AddEntityDefinition(kvp.Key, kvp.Value, entry.Name, entry.Modules, entry.Nested, entry.Archetype.TagSet);\n\t\t\t}\n\t\t}\n\n\t\tvoid ValidateNested()\n\t\t{\n\t\t\tforeach (var entry in _allDefinitionEntries)\n\t\t\t{\n\t\t\t\tbool parentIsPlaced = ModuleClosure.IsPlaced(entry.Modules);\n\t\t\t\tforeach (var n in entry.Nested)\n\t\t\t\t{\n\t\t\t\t\tvar nd = n.DefinitionAssetRef;\n\t\t\t\t\tif (nd == null)\n\t\t\t\t\t\tcontinue;\n\t\t\t\t\tvar def = nd.Definition;\n\t\t\t\t\tAssert.IsTrue(_byKey.ContainsKey(def.Id), $\"'{entry.Name}': nested '{def.Name}' was not declared\");\n\t\t\t\t\tAssert.IsTrue(parentIsPlaced || !ModuleClosure.IsPlaced(def.Modules), $\"'{entry.Name}': nests placed '{def.Name}' but has no transform to anchor it\");\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// We only need to validate keyed entries, because only keyed entries can be referenced (and thus be cyclic).\n\t\t\tvar state = new Dictionary<EntityDefKey, byte>(_byKey.Count);\n\t\t\tforeach (var kvp in _byKey)\n\t\t\t\tDetectSpawnCycle(kvp.Key, state);\n\t\t}\n\n\t\tvoid DetectSpawnCycle(EntityDefKey key, Dictionary<EntityDefKey, byte> state)\n\t\t{\n\t\t\tconst byte Visiting = 1, Done = 2;\n\n\t\t\tvar entry = _allDefinitionEntries[_byKey[key]];\n\t\t\tstate.TryGetValue(key, out var mark);\n\t\t\tAssert.IsTrue(mark != Visiting, $\"definition spawn cycle through '{entry.Name}'\");\n// … (truncated)"
     }
    },
    {
     "line": 207,
     "state": {
      "comment": "// A look shared by several definitions is walked once.",
      "annotated_code": "\t\t\tvar walked = new Dictionary<Look, LookSource>();\n\t\t\tvar looks = new LookSource[_allDefinitionEntries.Count];\n\t\t\tfor (int i = 0; i < _allDefinitionEntries.Count; i++)\n\t\t\t\tforeach (var config in _allDefinitionEntries[i].Modules)\n\t\t\t\t{\n\t\t\t\t\tif (config is not LookModuleConfig lookConfig || lookConfig.Look == null)\n\t\t\t\t\t\tcontinue;\n\n\t\t\t\t\tif (!walked.TryGetValue(lookConfig.Look, out var source))\n\t\t\t\t\t\twalked.Add(lookConfig.Look, source = lookConfig.Look.Walk());\n\t\t\t\t\tlooks[i] = source;\n\t\t\t\t}\n\n\t\t\treturn looks;"
     }
    },
    {
     "line": 224,
     "state": {
      "comment": "// Phase 3, after world build. Publishes every keyed entity definition into DefinitionRegistry.",
      "annotated_code": "\t\tpublic void BuildBlobRegistry(DefinitionRegistry definitions)\n\t\t{\n\t\t\tValidateNested();\n\t\t\tdefinitions.CreateDefinitionRegistry(_byKey.Count, _timelineCount);\n\t\t\tforeach (var kvp in _byKey)\n\t\t\t{\n\t\t\t\tvar entry = _allDefinitionEntries[kvp.Value];\n\t\t\t\tdefinitions.AddEntityDefinition(kvp.Key, kvp.Value, entry.Name, entry.Modules, entry.Nested, entry.Archetype.TagSet);\n\t\t\t}\n\t\t}\n\n\t\tvoid ValidateNested()\n\t\t{\n\t\t\tforeach (var entry in _allDefinitionEntries)\n\t\t\t{\n\t\t\t\tbool parentIsPlaced = ModuleClosure.IsPlaced(entry.Modules);\n\t\t\t\tforeach (var n in entry.Nested)\n\t\t\t\t{\n\t\t\t\t\tvar nd = n.DefinitionAssetRef;\n\t\t\t\t\tif (nd == null)\n\t\t\t\t\t\tcontinue;\n\t\t\t\t\tvar def = nd.Definition;\n\t\t\t\t\tAssert.IsTrue(_byKey.ContainsKey(def.Id), $\"'{entry.Name}': nested '{def.Name}' was not declared\");\n\t\t\t\t\tAssert.IsTrue(parentIsPlaced || !ModuleClosure.IsPlaced(def.Modules), $\"'{entry.Name}': nests placed '{def.Name}' but has no transform to anchor it\");\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// We only need to validate keyed entries, because only keyed entries can be referenced (and thus be cyclic).\n\t\t\tvar state = new Dictionary<EntityDefKey, byte>(_byKey.Count);\n\t\t\tforeach (var kvp in _byKey)\n\t\t\t\tDetectSpawnCycle(kvp.Key, state);\n\t\t}\n\n\t\tvoid DetectSpawnCycle(EntityDefKey key, Dictionary<EntityDefKey, byte> state)\n\t\t{\n\t\t\tconst byte Visiting = 1, Done = 2;\n\n\t\t\tvar entry = _allDefinitionEntries[_byKey[key]];\n\t\t\tstate.TryGetValue(key, out var mark);\n\t\t\tAssert.IsTrue(mark != Visiting, $\"definition spawn cycle through '{entry.Name}'\");\n\t\t\tif (mark == Done)\n\t\t\t\treturn;\n\n\t\t\tstate[key] = Visiting;\n\t\t\tforeach (var nested in entry.Nested)\n\t\t\t{\n\t\t\t\tvar n = nested.DefinitionAssetRef?.Definition;\n\t\t\t\tif (n != null && _byKey.ContainsKey(n.Id))\n\t\t\t\t\tDetectSpawnCycle(n.Id, state);\n\t\t\t}\n\t\t\tforeach (var config in entry.Modules)\n\t\t\t{\n\t\t\t\tif (config is not AbilityOwnerModuleConfig owner)\n\t\t\t\t\tcontinue;\n\t\t\t\tfor (int list = 0; list < 2; list++)\n\t\t\t\t\tforeach (var asset in list == 0 ? owner.Ordered : owner.Auto)\n\t\t\t\t\t{\n\t\t\t\t\t\tvar a = asset?.Definition;\n\t\t\t\t\t\tif (a != null && _byKey.ContainsKey(a.Id))\n\t\t\t\t\t\t\tDetectSpawnCycle(a.Id, state);\n// … (truncated)"
     }
    },
    {
     "line": 252,
     "state": {
      "comment": "// We only need to validate keyed entries, because only keyed entries can be referenced (and thus be cyclic).",
      "annotated_code": "\t\t\tvar state = new Dictionary<EntityDefKey, byte>(_byKey.Count);\n\t\t\tforeach (var kvp in _byKey)\n\t\t\t\tDetectSpawnCycle(kvp.Key, state);"
     }
    }
   ],
   "declarations": [
    {
     "line": 30,
     "head": "static readonly IReadOnlyList<NestedDefinition> NoNested = Array.Empty<NestedDefinition>();",
     "state": {
      "declaration": "\t\tstatic readonly IReadOnlyList<NestedDefinition> NoNested = Array.Empty<NestedDefinition>();",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 32,
     "head": "readonly TemplateRegistry _registry = new();",
     "state": {
      "declaration": "\t\treadonly TemplateRegistry _registry = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 34,
     "head": "readonly List<DefinitionEntry> _allDefinitionEntries = new();",
     "state": {
      "declaration": "\t\treadonly List<DefinitionEntry> _allDefinitionEntries = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 37,
     "head": "readonly Dictionary<EntityDefKey, int> _byKey = new();",
     "state": {
      "declaration": "\t\treadonly Dictionary<EntityDefKey, int> _byKey = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Published subset.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 40,
     "head": "readonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();",
     "state": {
      "declaration": "\t\treadonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Dedups timeline nodes.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 42,
     "head": "readonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();",
     "state": {
      "declaration": "\t\treadonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 44,
     "head": "readonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();",
     "state": {
      "declaration": "\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 46,
     "head": "int _generatedCounter;",
     "state": {
      "declaration": "\t\tint _generatedCounter;",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 47,
     "head": "int _timelineCount;",
     "state": {
      "declaration": "\t\tint _timelineCount;",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 50,
     "head": "internal void DeclareCollection(EntityDefinitionCollection collection)",
     "state": {
      "declaration": "\t\tinternal void DeclareCollection(EntityDefinitionCollection collection)\n\t\t{\n\t\t\tAssert.IsNotNull(collection, \"DeclareCollection: null collection\");\n\t\t\tforeach (var asset in collection.Definitions)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(asset, $\"'{collection.name}': empty definition entry\");\n\t\t\t\tDeclareDefinitionAsset(asset);\n\t\t\t}\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Phase 1, before world build.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 61,
     "head": "internal TemplateRegistry.RegisteredArchetype DeclareUnkeyed(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)",
     "state": {
      "declaration": "\t\tinternal TemplateRegistry.RegisteredArchetype DeclareUnkeyed(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = DeclareEntry(nameHint, configs, nested);\n\t\t\treturn _allDefinitionEntries[entry].Archetype;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Phase 1, before world build.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 68,
     "head": "public IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();",
     "state": {
      "declaration": "\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Phase 2.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 70,
     "head": "void DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)",
     "state": {
      "declaration": "\t\tvoid DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _byKey[key] = DeclareEntry(nameHint, configs, nested);",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 75,
     "head": "int DeclareEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)",
     "state": {
      "declaration": "\t\tint DeclareEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tDeclareAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tDeclareTimeline(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = DeclareEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _allDefinitionEntries.Count;\n\t\t\t_allDefinitionEntries.Add(new DefinitionEntry(nameHint, configs, nested ?? NoNested, archetype));\n\t\t\tDeclareNested(nested);\n\t\t\treturn index;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Takes ownership of configs and the configs in it: augments the list, modifies the configs.\n// Returns entry index: module walk (before) and nested declare (after) append their own entries.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 105,
     "head": "void DeclareAbilities(AbilityOwnerModuleConfig owner)",
     "state": {
      "declaration": "\t\tvoid DeclareAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = DeclareDefinitionAssets(owner.Ordered);\n\t\t\towner.ResolvedAuto = DeclareDefinitionAssets(owner.Auto);\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 112,
     "head": "EntityDefKey[] DeclareDefinitionAssets(EntityDefinitionAsset[] assets)",
     "state": {
      "declaration": "\t\tEntityDefKey[] DeclareDefinitionAssets(EntityDefinitionAsset[] assets)\n\t\t{\n\t\t\tvar keys = new EntityDefKey[assets.Length];\n\t\t\tfor (int i = 0; i < assets.Length; i++)\n\t\t\t\tif (assets[i] != null)\n\t\t\t\t\tkeys[i] = DeclareDefinitionAsset(assets[i]);\n\t\t\treturn keys;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Null entries keep their slot with a default key.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 121,
     "head": "EntityDefKey DeclareEntityCreate(EntityCreateConfig config, string context)",
     "state": {
      "declaration": "\t\tEntityDefKey DeclareEntityCreate(EntityCreateConfig config, string context)\n\t\t{\n\t\t\t// Asset ref: reuses the asset's key.\n\t\t\tif (config.DefinitionAsset != null)\n\t\t\t\treturn DeclareDefinitionAsset(config.DefinitionAsset);\n\n\t\t\t// Inline: generated key, deduped by config identity.\n\t\t\tif (_inlineCreateKeys.TryGetValue(config, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = config.InlineDefinition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\t\t\tif (def.Modules == null || def.Modules.Count == 0)\n\t\t\t\treturn default; // Validate reports it; nothing to declare\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_inlineCreateKeys[config] = key; // set before recursion — guards a definition spawning itself\n\t\t\tDeclareKeyed(key, context, configs, def.Nested);\n\t\t\treturn key;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 143,
     "head": "internal EntityDefKey DeclareDefinitionAsset(EntityDefinitionAsset asset)",
     "state": {
      "declaration": "\t\tinternal EntityDefKey DeclareDefinitionAsset(EntityDefinitionAsset asset)\n\t\t{\n\t\t\tAssert.IsNotNull(asset, \"declare definition: null asset\");\n\t\t\tif (_assetKeys.TryGetValue(asset, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar definition = asset.Definition;\n\t\t\tAssert.IsNotNull(definition, $\"'{asset.name}': empty definition\");\n\t\t\tAssert.IsFalse(definition.Id.IsNone, $\"'{asset.name}': definition without id\");\n\t\t\tAssert.IsFalse(definition.Id.IsGenerated, $\"'{asset.name}': asset id in the compiler-minted range\");\n\t\t\tAssert.IsFalse(_byKey.ContainsKey(definition.Id), $\"'{asset.name}': duplicate definition id {definition.Id}\");\n\n\t\t\t_assetKeys[asset] = definition.Id; // set before recursion — guards a definition reaching itself\n\n\t\t\t// Copy: declare augments the list; the asset's serialized modules must stay untouched.\n\t\t\tvar configs = new List<ModuleConfig>(definition.Modules);\n\t\t\tDeclareKeyed(definition.Id, asset.name, configs, definition.Nested);\n\t\t\treturn definition.Id;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 163,
     "head": "void DeclareNested(IReadOnlyList<NestedDefinition> nested)",
     "state": {
      "declaration": "\t\tvoid DeclareNested(IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tif (nested == null)\n\t\t\t\treturn;\n\t\t\tforeach (var n in nested)\n\t\t\t\tif (n.DefinitionAssetRef != null)\n\t\t\t\t\tDeclareDefinitionAsset(n.DefinitionAssetRef);\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 172,
     "head": "void DeclareTimeline(ActionTimelineModuleConfig timeline, string context)",
     "state": {
      "declaration": "\t\tvoid DeclareTimeline(ActionTimelineModuleConfig timeline, string context)\n\t\t{\n\t\t\t_timelineCount++;\n\t\t\tfor (int i = 0; i < timeline.Nodes.Count; i++)\n\t\t\t{\n\t\t\t\tvar node = timeline.Nodes[i];\n\t\t\t\tAssert.IsNotNull(node, $\"'{context}': empty node entry\");\n\t\t\t\tnode.ResolvedDefinition = DeclareTimelineNode(node, $\"{context}[{i}]\");\n\t\t\t}\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 183,
     "head": "EntityDefKey DeclareTimelineNode(TimelineNodeConfig node, string context)",
     "state": {
      "declaration": "\t\tEntityDefKey DeclareTimelineNode(TimelineNodeConfig node, string context)\n\t\t{\n\t\t\tif (_nodeKeys.TryGetValue(node, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = node.Definition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tModuleClosure.Ensure(configs, EntityModules.ConditionTime, () => new ConditionTimeModuleConfig());\n\n\t\t\tAssert.IsFalse(ModuleClosure.IsPlaced(configs), $\"'{context}': timeline node has a transform, but timeline nodes spawn without a transform anchor\");\n\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_nodeKeys[node] = key; // set before recursion — guards re-entry\n\t\t\tDeclareKeyed(key, node.DefinitionAsset != null ? $\"{context} ▸ {node.DefinitionAsset.name}\" : context, configs, def.Nested);\n\t\t\treturn key;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 202,
     "head": "EntityDefKey MintGeneratedKey() => EntityDefKey.Generated(_generatedCounter++);",
     "state": {
      "declaration": "\t\tEntityDefKey MintGeneratedKey() => EntityDefKey.Generated(_generatedCounter++);",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 205,
     "head": "public IReadOnlyList<LookSource> BuildLooks()",
     "state": {
      "declaration": "\t\tpublic IReadOnlyList<LookSource> BuildLooks()\n\t\t{\n\t\t\t// A look shared by several definitions is walked once.\n\t\t\tvar walked = new Dictionary<Look, LookSource>();\n\t\t\tvar looks = new LookSource[_allDefinitionEntries.Count];\n\t\t\tfor (int i = 0; i < _allDefinitionEntries.Count; i++)\n\t\t\t\tforeach (var config in _allDefinitionEntries[i].Modules)\n\t\t\t\t{\n\t\t\t\t\tif (config is not LookModuleConfig lookConfig || lookConfig.Look == null)\n\t\t\t\t\t\tcontinue;\n\n\t\t\t\t\tif (!walked.TryGetValue(lookConfig.Look, out var source))\n\t\t\t\t\t\twalked.Add(lookConfig.Look, source = lookConfig.Look.Walk());\n\t\t\t\t\tlooks[i] = source;\n\t\t\t\t}\n\n\t\t\treturn looks;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// By entry, null for none.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 225,
     "head": "public void BuildBlobRegistry(DefinitionRegistry definitions)",
     "state": {
      "declaration": "\t\tpublic void BuildBlobRegistry(DefinitionRegistry definitions)\n\t\t{\n\t\t\tValidateNested();\n\t\t\tdefinitions.CreateDefinitionRegistry(_byKey.Count, _timelineCount);\n\t\t\tforeach (var kvp in _byKey)\n\t\t\t{\n\t\t\t\tvar entry = _allDefinitionEntries[kvp.Value];\n\t\t\t\tdefinitions.AddEntityDefinition(kvp.Key, kvp.Value, entry.Name, entry.Modules, entry.Nested, entry.Archetype.TagSet);\n\t\t\t}\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Phase 3, after world build. Publishes every keyed entity definition into DefinitionRegistry.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 236,
     "head": "void ValidateNested()",
     "state": {
      "declaration": "\t\tvoid ValidateNested()\n\t\t{\n\t\t\tforeach (var entry in _allDefinitionEntries)\n\t\t\t{\n\t\t\t\tbool parentIsPlaced = ModuleClosure.IsPlaced(entry.Modules);\n\t\t\t\tforeach (var n in entry.Nested)\n\t\t\t\t{\n\t\t\t\t\tvar nd = n.DefinitionAssetRef;\n\t\t\t\t\tif (nd == null)\n\t\t\t\t\t\tcontinue;\n\t\t\t\t\tvar def = nd.Definition;\n\t\t\t\t\tAssert.IsTrue(_byKey.ContainsKey(def.Id), $\"'{entry.Name}': nested '{def.Name}' was not declared\");\n\t\t\t\t\tAssert.IsTrue(parentIsPlaced || !ModuleClosure.IsPlaced(def.Modules), $\"'{entry.Name}': nests placed '{def.Name}' but has no transform to anchor it\");\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// We only need to validate keyed entries, because only keyed entries can be referenced (and thus be cyclic).\n\t\t\tvar state = new Dictionary<EntityDefKey, byte>(_byKey.Count);\n\t\t\tforeach (var kvp in _byKey)\n\t\t\t\tDetectSpawnCycle(kvp.Key, state);\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 258,
     "head": "void DetectSpawnCycle(EntityDefKey key, Dictionary<EntityDefKey, byte> state)",
     "state": {
      "declaration": "\t\tvoid DetectSpawnCycle(EntityDefKey key, Dictionary<EntityDefKey, byte> state)\n\t\t{\n\t\t\tconst byte Visiting = 1, Done = 2;\n\n\t\t\tvar entry = _allDefinitionEntries[_byKey[key]];\n\t\t\tstate.TryGetValue(key, out var mark);\n\t\t\tAssert.IsTrue(mark != Visiting, $\"definition spawn cycle through '{entry.Name}'\");\n\t\t\tif (mark == Done)\n\t\t\t\treturn;\n\n\t\t\tstate[key] = Visiting;\n\t\t\tforeach (var nested in entry.Nested)\n\t\t\t{\n\t\t\t\tvar n = nested.DefinitionAssetRef?.Definition;\n\t\t\t\tif (n != null && _byKey.ContainsKey(n.Id))\n\t\t\t\t\tDetectSpawnCycle(n.Id, state);\n\t\t\t}\n\t\t\tforeach (var config in entry.Modules)\n\t\t\t{\n\t\t\t\tif (config is not AbilityOwnerModuleConfig owner)\n\t\t\t\t\tcontinue;\n\t\t\t\tfor (int list = 0; list < 2; list++)\n\t\t\t\t\tforeach (var asset in list == 0 ? owner.Ordered : owner.Auto)\n\t\t\t\t\t{\n\t\t\t\t\t\tvar a = asset?.Definition;\n\t\t\t\t\t\tif (a != null && _byKey.ContainsKey(a.Id))\n\t\t\t\t\t\t\tDetectSpawnCycle(a.Id, state);\n\t\t\t\t\t}\n\t\t\t}\n\t\t\tstate[key] = Done;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_byKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareCollection",
       "DeclareUnkeyed",
       "BuildTemplates",
       "DeclareKeyed",
       "DeclareEntry",
       "DeclareAbilities",
       "DeclareDefinitionAssets",
       "DeclareEntityCreate",
       "DeclareDefinitionAsset",
       "DeclareNested",
       "DeclareTimeline",
       "DeclareTimelineNode",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "ValidateNested",
       "DetectSpawnCycle"
      ]
     }
    },
    {
     "line": 16,
     "head": "public readonly string Name;",
     "state": {
      "declaration": "\t\t\tpublic readonly string Name;",
      "type_signature": "readonly struct DefinitionEntry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "DefinitionEntry"
      ]
     }
    },
    {
     "line": 17,
     "head": "public readonly IReadOnlyList<ModuleConfig> Modules;",
     "state": {
      "declaration": "\t\t\tpublic readonly IReadOnlyList<ModuleConfig> Modules;",
      "type_signature": "readonly struct DefinitionEntry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "DefinitionEntry"
      ]
     }
    },
    {
     "line": 18,
     "head": "public readonly IReadOnlyList<NestedDefinition> Nested;",
     "state": {
      "declaration": "\t\t\tpublic readonly IReadOnlyList<NestedDefinition> Nested;",
      "type_signature": "readonly struct DefinitionEntry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "DefinitionEntry"
      ]
     }
    },
    {
     "line": 19,
     "head": "public readonly TemplateRegistry.RegisteredArchetype Archetype;",
     "state": {
      "declaration": "\t\t\tpublic readonly TemplateRegistry.RegisteredArchetype Archetype;",
      "type_signature": "readonly struct DefinitionEntry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "DefinitionEntry"
      ]
     }
    },
    {
     "line": 21,
     "head": "public DefinitionEntry(string name, IReadOnlyList<ModuleConfig> modules, IReadOnlyList<NestedDefinition> nested, TemplateRegistry.RegisteredArchetype archetype)",
     "state": {
      "declaration": "\t\t\tpublic DefinitionEntry(string name, IReadOnlyList<ModuleConfig> modules, IReadOnlyList<NestedDefinition> nested, TemplateRegistry.RegisteredArchetype archetype)\n\t\t\t{\n\t\t\t\tName = name;\n\t\t\t\tModules = modules;\n\t\t\t\tNested = nested;\n\t\t\t\tArchetype = archetype;\n\t\t\t}",
      "type_signature": "readonly struct DefinitionEntry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "DefinitionEntry"
      ]
     }
    }
   ],
   "types": [
    {
     "line": 12,
     "head": "public sealed class DefinitionCompiler",
     "state": {
      "signatures": [
       "internal void DeclareCollection(EntityDefinitionCollection collection)",
       "internal TemplateRegistry.RegisteredArchetype DeclareUnkeyed(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)",
       "public IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();",
       "void DeclareKeyed(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)",
       "int DeclareEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)",
       "void DeclareAbilities(AbilityOwnerModuleConfig owner)",
       "EntityDefKey[] DeclareDefinitionAssets(EntityDefinitionAsset[] assets)",
       "EntityDefKey DeclareEntityCreate(EntityCreateConfig config, string context)",
       "internal EntityDefKey DeclareDefinitionAsset(EntityDefinitionAsset asset)",
       "void DeclareNested(IReadOnlyList<NestedDefinition> nested)",
       "void DeclareTimeline(ActionTimelineModuleConfig timeline, string context)",
       "EntityDefKey DeclareTimelineNode(TimelineNodeConfig node, string context)",
       "EntityDefKey MintGeneratedKey() => EntityDefKey.Generated(_generatedCounter++);",
       "public IReadOnlyList<LookSource> BuildLooks()",
       "public void BuildBlobRegistry(DefinitionRegistry definitions)",
       "void ValidateNested()",
       "void DetectSpawnCycle(EntityDefKey key, Dictionary<EntityDefKey, byte> state)"
      ]
     }
    },
    {
     "line": 14,
     "head": "readonly struct DefinitionEntry",
     "state": {
      "signatures": [
       "public DefinitionEntry(string name, IReadOnlyList<ModuleConfig> modules, IReadOnlyList<NestedDefinition> nested, TemplateRegistry.RegisteredArchetype archetype)"
      ]
     }
    }
   ]
  },
  "v2": {
   "comment_blocks": [
    {
     "line": 8,
     "state": {
      "comment": "// Authored compositions -> runtime archetypes + definition blobs. Phases, in order:\n// 1. DeclareUnkeyedDefinition — before world build: registers archetypes for every reachable definition.\n// 2. BuildTemplates — before world build.\n// 3. BuildBlobRegistry — after world build: publishes keyed definitions into DefinitionRegistry.",
      "annotated_code": "\tpublic sealed class DefinitionCompiler\n\t{\n\t\treadonly struct DefinitionEntry\n\t\t{\n\t\t\tpublic readonly string Name;\n\t\t\tpublic readonly IReadOnlyList<ModuleConfig> Modules;\n\t\t\tpublic readonly IReadOnlyList<NestedDefinition> Nested;\n\t\t\tpublic readonly TemplateRegistry.RegisteredArchetype Archetype;\n\n\t\t\tpublic DefinitionEntry(string name, IReadOnlyList<ModuleConfig> modules, IReadOnlyList<NestedDefinition> nested, TemplateRegistry.RegisteredArchetype archetype)\n\t\t\t{\n\t\t\t\tName = name;\n\t\t\t\tModules = modules;\n\t\t\t\tNested = nested;\n\t\t\t\tArchetype = archetype;\n\t\t\t}\n\t\t}\n\n\t\tstatic readonly IReadOnlyList<NestedDefinition> NoNested = Array.Empty<NestedDefinition>();\n\n\t\treadonly TemplateRegistry _registry = new();\n\n\t\treadonly List<DefinitionEntry> _allDefinitionEntries = new();\n\n\t\t// Published subset.\n\t\treadonly Dictionary<EntityDefKey, int> _entryIndexByKey = new();\n\n\t\t// Dedups timeline nodes.\n\t\treadonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();\n\n\t\treadonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();\n\n\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();\n\n\t\tint _generatedCounter;\n\t\tint _timelineCount;\n\n\t\t// Phase 1, before world build.\n\t\tinternal TemplateRegistry.RegisteredArchetype DeclareUnkeyedDefinition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = CreateEntry(nameHint, configs, nested);\n\t\t\treturn _allDefinitionEntries[entry].Archetype;\n\t\t}\n\n\t\t// Phase 2.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\tint DeclareKeyedDefinition(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _entryIndexByKey[key] = CreateEntry(nameHint, configs, nested);\n\n\t\t// Takes ownership of configs and the configs in it: augments the list, modifies the configs.\n\t\t// Returns entry index: module walk (before) and nested declare (after) append their own entries.\n\t\tint CreateEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tnested ??= NoNested;\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n// … (truncated)"
     }
    },
    {
     "line": 36,
     "state": {
      "comment": "// Published subset.",
      "annotated_code": "\t\treadonly Dictionary<EntityDefKey, int> _entryIndexByKey = new();\n\n\t\t// Dedups timeline nodes.\n\t\treadonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();\n\n\t\treadonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();\n\n\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();\n\n\t\tint _generatedCounter;\n\t\tint _timelineCount;\n\n\t\t// Phase 1, before world build.\n\t\tinternal TemplateRegistry.RegisteredArchetype DeclareUnkeyedDefinition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = CreateEntry(nameHint, configs, nested);\n\t\t\treturn _allDefinitionEntries[entry].Archetype;\n\t\t}\n\n\t\t// Phase 2.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\tint DeclareKeyedDefinition(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _entryIndexByKey[key] = CreateEntry(nameHint, configs, nested);\n\n\t\t// Takes ownership of configs and the configs in it: augments the list, modifies the configs.\n\t\t// Returns entry index: module walk (before) and nested declare (after) append their own entries.\n\t\tint CreateEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tnested ??= NoNested;\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tResolveAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tResolveTimeline(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = InternEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _allDefinitionEntries.Count;\n\t\t\t_allDefinitionEntries.Add(new DefinitionEntry(nameHint, configs, nested, archetype));\n\t\t\tforeach (var n in nested)\n\t\t\t\tif (n.DefinitionAssetRef != null)\n\t\t\t\t\tInternDefinitionAsset(n.DefinitionAssetRef);\n\t\t\treturn index;\n\t\t}\n\n// … (truncated)"
     }
    },
    {
     "line": 39,
     "state": {
      "comment": "// Dedups timeline nodes.",
      "annotated_code": "\t\treadonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();\n\n\t\treadonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();\n\n\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();\n\n\t\tint _generatedCounter;\n\t\tint _timelineCount;\n\n\t\t// Phase 1, before world build.\n\t\tinternal TemplateRegistry.RegisteredArchetype DeclareUnkeyedDefinition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = CreateEntry(nameHint, configs, nested);\n\t\t\treturn _allDefinitionEntries[entry].Archetype;\n\t\t}\n\n\t\t// Phase 2.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\tint DeclareKeyedDefinition(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _entryIndexByKey[key] = CreateEntry(nameHint, configs, nested);\n\n\t\t// Takes ownership of configs and the configs in it: augments the list, modifies the configs.\n\t\t// Returns entry index: module walk (before) and nested declare (after) append their own entries.\n\t\tint CreateEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tnested ??= NoNested;\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tResolveAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tResolveTimeline(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = InternEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _allDefinitionEntries.Count;\n\t\t\t_allDefinitionEntries.Add(new DefinitionEntry(nameHint, configs, nested, archetype));\n\t\t\tforeach (var n in nested)\n\t\t\t\tif (n.DefinitionAssetRef != null)\n\t\t\t\t\tInternDefinitionAsset(n.DefinitionAssetRef);\n\t\t\treturn index;\n\t\t}\n\n\t\tvoid ResolveAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = InternDefinitionAssets(owner.Ordered);\n// … (truncated)"
     }
    },
    {
     "line": 49,
     "state": {
      "comment": "// Phase 1, before world build.",
      "annotated_code": "\t\tinternal TemplateRegistry.RegisteredArchetype DeclareUnkeyedDefinition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = CreateEntry(nameHint, configs, nested);\n\t\t\treturn _allDefinitionEntries[entry].Archetype;\n\t\t}\n\n\t\t// Phase 2.\n\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\tint DeclareKeyedDefinition(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _entryIndexByKey[key] = CreateEntry(nameHint, configs, nested);\n\n\t\t// Takes ownership of configs and the configs in it: augments the list, modifies the configs.\n\t\t// Returns entry index: module walk (before) and nested declare (after) append their own entries.\n\t\tint CreateEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tnested ??= NoNested;\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tResolveAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tResolveTimeline(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = InternEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _allDefinitionEntries.Count;\n\t\t\t_allDefinitionEntries.Add(new DefinitionEntry(nameHint, configs, nested, archetype));\n\t\t\tforeach (var n in nested)\n\t\t\t\tif (n.DefinitionAssetRef != null)\n\t\t\t\t\tInternDefinitionAsset(n.DefinitionAssetRef);\n\t\t\treturn index;\n\t\t}\n\n\t\tvoid ResolveAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = InternDefinitionAssets(owner.Ordered);\n\t\t\towner.ResolvedAuto = InternDefinitionAssets(owner.Auto);\n\t\t}\n\n\t\t// Null entries keep their slot with a default key.\n\t\tEntityDefKey[] InternDefinitionAssets(EntityDefinitionAsset[] assets)\n\t\t{\n\t\t\tvar keys = new EntityDefKey[assets.Length];\n\t\t\tfor (int i = 0; i < assets.Length; i++)\n\t\t\t\tif (assets[i] != null)\n\t\t\t\t\tkeys[i] = InternDefinitionAsset(assets[i]);\n// … (truncated)"
     }
    },
    {
     "line": 56,
     "state": {
      "comment": "// Phase 2.",
      "annotated_code": "\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();\n\n\t\tint DeclareKeyedDefinition(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _entryIndexByKey[key] = CreateEntry(nameHint, configs, nested);\n\n\t\t// Takes ownership of configs and the configs in it: augments the list, modifies the configs.\n\t\t// Returns entry index: module walk (before) and nested declare (after) append their own entries.\n\t\tint CreateEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tnested ??= NoNested;\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tResolveAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tResolveTimeline(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = InternEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _allDefinitionEntries.Count;\n\t\t\t_allDefinitionEntries.Add(new DefinitionEntry(nameHint, configs, nested, archetype));\n\t\t\tforeach (var n in nested)\n\t\t\t\tif (n.DefinitionAssetRef != null)\n\t\t\t\t\tInternDefinitionAsset(n.DefinitionAssetRef);\n\t\t\treturn index;\n\t\t}\n\n\t\tvoid ResolveAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = InternDefinitionAssets(owner.Ordered);\n\t\t\towner.ResolvedAuto = InternDefinitionAssets(owner.Auto);\n\t\t}\n\n\t\t// Null entries keep their slot with a default key.\n\t\tEntityDefKey[] InternDefinitionAssets(EntityDefinitionAsset[] assets)\n\t\t{\n\t\t\tvar keys = new EntityDefKey[assets.Length];\n\t\t\tfor (int i = 0; i < assets.Length; i++)\n\t\t\t\tif (assets[i] != null)\n\t\t\t\t\tkeys[i] = InternDefinitionAsset(assets[i]);\n\t\t\treturn keys;\n\t\t}\n\n\t\tEntityDefKey InternEntityCreate(EntityCreateConfig config, string context)\n\t\t{\n\t\t\t// Asset ref: reuses the asset's key.\n\t\t\tif (config.DefinitionAsset != null)\n// … (truncated)"
     }
    },
    {
     "line": 62,
     "state": {
      "comment": "// Takes ownership of configs and the configs in it: augments the list, modifies the configs.\n// Returns entry index: module walk (before) and nested declare (after) append their own entries.",
      "annotated_code": "\t\tint CreateEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tnested ??= NoNested;\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tResolveAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tResolveTimeline(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = InternEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _allDefinitionEntries.Count;\n\t\t\t_allDefinitionEntries.Add(new DefinitionEntry(nameHint, configs, nested, archetype));\n\t\t\tforeach (var n in nested)\n\t\t\t\tif (n.DefinitionAssetRef != null)\n\t\t\t\t\tInternDefinitionAsset(n.DefinitionAssetRef);\n\t\t\treturn index;\n\t\t}\n\n\t\tvoid ResolveAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = InternDefinitionAssets(owner.Ordered);\n\t\t\towner.ResolvedAuto = InternDefinitionAssets(owner.Auto);\n\t\t}\n\n\t\t// Null entries keep their slot with a default key.\n\t\tEntityDefKey[] InternDefinitionAssets(EntityDefinitionAsset[] assets)\n\t\t{\n\t\t\tvar keys = new EntityDefKey[assets.Length];\n\t\t\tfor (int i = 0; i < assets.Length; i++)\n\t\t\t\tif (assets[i] != null)\n\t\t\t\t\tkeys[i] = InternDefinitionAsset(assets[i]);\n\t\t\treturn keys;\n\t\t}\n\n\t\tEntityDefKey InternEntityCreate(EntityCreateConfig config, string context)\n\t\t{\n\t\t\t// Asset ref: reuses the asset's key.\n\t\t\tif (config.DefinitionAsset != null)\n\t\t\t\treturn InternDefinitionAsset(config.DefinitionAsset);\n\n\t\t\t// Inline: generated key, deduped by config identity.\n\t\t\tif (_inlineCreateKeys.TryGetValue(config, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = config.InlineDefinition;\n// … (truncated)"
     }
    },
    {
     "line": 103,
     "state": {
      "comment": "// Null entries keep their slot with a default key.",
      "annotated_code": "\t\tEntityDefKey[] InternDefinitionAssets(EntityDefinitionAsset[] assets)\n\t\t{\n\t\t\tvar keys = new EntityDefKey[assets.Length];\n\t\t\tfor (int i = 0; i < assets.Length; i++)\n\t\t\t\tif (assets[i] != null)\n\t\t\t\t\tkeys[i] = InternDefinitionAsset(assets[i]);\n\t\t\treturn keys;\n\t\t}\n\n\t\tEntityDefKey InternEntityCreate(EntityCreateConfig config, string context)\n\t\t{\n\t\t\t// Asset ref: reuses the asset's key.\n\t\t\tif (config.DefinitionAsset != null)\n\t\t\t\treturn InternDefinitionAsset(config.DefinitionAsset);\n\n\t\t\t// Inline: generated key, deduped by config identity.\n\t\t\tif (_inlineCreateKeys.TryGetValue(config, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = config.InlineDefinition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\t\t\tif (def.Modules == null || def.Modules.Count == 0)\n\t\t\t\treturn default; // Validate reports it; nothing to declare\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_inlineCreateKeys[config] = key; // set before recursion — guards a definition spawning itself\n\t\t\tDeclareKeyedDefinition(key, context, configs, def.Nested);\n\t\t\treturn key;\n\t\t}\n\n\t\tinternal EntityDefKey InternDefinitionAsset(EntityDefinitionAsset asset)\n\t\t{\n\t\t\tAssert.IsNotNull(asset, \"declare definition: null asset\");\n\t\t\tif (_assetKeys.TryGetValue(asset, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar definition = asset.Definition;\n\t\t\tAssert.IsNotNull(definition, $\"'{asset.name}': empty definition\");\n\t\t\tAssert.IsFalse(definition.Id.IsNone, $\"'{asset.name}': definition without id\");\n\t\t\tAssert.IsFalse(definition.Id.IsGenerated, $\"'{asset.name}': asset id in the compiler-minted range\");\n\t\t\tAssert.IsFalse(_entryIndexByKey.ContainsKey(definition.Id), $\"'{asset.name}': duplicate definition id {definition.Id}\");\n\n\t\t\t_assetKeys[asset] = definition.Id; // set before recursion — guards a definition reaching itself\n\n\t\t\t// Copy: declare augments the list; the asset's serialized modules must stay untouched.\n\t\t\tvar configs = new List<ModuleConfig>(definition.Modules);\n\t\t\tDeclareKeyedDefinition(definition.Id, asset.name, configs, definition.Nested);\n\t\t\treturn definition.Id;\n\t\t}\n\n\t\tvoid ResolveTimeline(ActionTimelineModuleConfig timeline, string context)\n\t\t{\n\t\t\t_timelineCount++;\n\t\t\tfor (int i = 0; i < timeline.Nodes.Count; i++)\n\t\t\t{\n\t\t\t\tvar node = timeline.Nodes[i];\n\t\t\t\tAssert.IsNotNull(node, $\"'{context}': empty node entry\");\n\t\t\t\tnode.ResolvedDefinition = InternTimelineNode(node, $\"{context}[{i}]\");\n\t\t\t}\n// … (truncated)"
     }
    },
    {
     "line": 115,
     "state": {
      "comment": "// Asset ref: reuses the asset's key.",
      "annotated_code": "\t\t\tif (config.DefinitionAsset != null)\n\t\t\t\treturn InternDefinitionAsset(config.DefinitionAsset);\n\n\t\t\t// Inline: generated key, deduped by config identity.\n\t\t\tif (_inlineCreateKeys.TryGetValue(config, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = config.InlineDefinition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\t\t\tif (def.Modules == null || def.Modules.Count == 0)\n\t\t\t\treturn default; // Validate reports it; nothing to declare\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_inlineCreateKeys[config] = key; // set before recursion — guards a definition spawning itself\n\t\t\tDeclareKeyedDefinition(key, context, configs, def.Nested);\n\t\t\treturn key;"
     }
    },
    {
     "line": 119,
     "state": {
      "comment": "// Inline: generated key, deduped by config identity.",
      "annotated_code": "\t\t\tif (_inlineCreateKeys.TryGetValue(config, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = config.InlineDefinition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\t\t\tif (def.Modules == null || def.Modules.Count == 0)\n\t\t\t\treturn default; // Validate reports it; nothing to declare\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_inlineCreateKeys[config] = key; // set before recursion — guards a definition spawning itself\n\t\t\tDeclareKeyedDefinition(key, context, configs, def.Nested);\n\t\t\treturn key;"
     }
    },
    {
     "line": 149,
     "state": {
      "comment": "// Copy: declare augments the list; the asset's serialized modules must stay untouched.",
      "annotated_code": "\t\t\tvar configs = new List<ModuleConfig>(definition.Modules);\n\t\t\tDeclareKeyedDefinition(definition.Id, asset.name, configs, definition.Nested);\n\t\t\treturn definition.Id;"
     }
    },
    {
     "line": 192,
     "state": {
      "comment": "// By entry, null for none.",
      "annotated_code": "\t\tpublic IReadOnlyList<LookSource> BuildLooks()\n\t\t{\n\t\t\t// A look shared by several definitions is walked once.\n\t\t\tvar walked = new Dictionary<Look, LookSource>();\n\t\t\tvar looks = new LookSource[_allDefinitionEntries.Count];\n\t\t\tfor (int i = 0; i < _allDefinitionEntries.Count; i++)\n\t\t\t\tforeach (var config in _allDefinitionEntries[i].Modules)\n\t\t\t\t{\n\t\t\t\t\tif (config is not LookModuleConfig lookConfig || lookConfig.Look == null)\n\t\t\t\t\t\tcontinue;\n\n\t\t\t\t\tif (!walked.TryGetValue(lookConfig.Look, out var source))\n\t\t\t\t\t\twalked.Add(lookConfig.Look, source = lookConfig.Look.Walk());\n\t\t\t\t\tlooks[i] = source;\n\t\t\t\t}\n\n\t\t\treturn looks;\n\t\t}\n\n\t\t// Phase 3, after world build. Publishes every keyed entity definition into DefinitionRegistry.\n\t\tpublic void BuildBlobRegistry(DefinitionRegistry definitions)\n\t\t{\n\t\t\tAssertDeclarationsValid();\n\t\t\tdefinitions.CreateDefinitionRegistry(_entryIndexByKey.Count, _timelineCount);\n\t\t\tforeach (var kvp in _entryIndexByKey)\n\t\t\t{\n\t\t\t\tvar entry = _allDefinitionEntries[kvp.Value];\n\t\t\t\tdefinitions.AddEntityDefinition(kvp.Key, kvp.Value, entry.Name, entry.Modules, entry.Nested, entry.Archetype.TagSet);\n\t\t\t}\n\t\t}\n\n\t\tvoid AssertDeclarationsValid()\n\t\t{\n\t\t\tforeach (var entry in _allDefinitionEntries)\n\t\t\t{\n\t\t\t\tbool parentIsPlaced = ModuleClosure.IsPlaced(entry.Modules);\n\t\t\t\tforeach (var n in entry.Nested)\n\t\t\t\t{\n\t\t\t\t\tvar nd = n.DefinitionAssetRef;\n\t\t\t\t\tif (nd == null)\n\t\t\t\t\t\tcontinue;\n\t\t\t\t\tvar def = nd.Definition;\n\t\t\t\t\tAssert.IsTrue(_entryIndexByKey.ContainsKey(def.Id), $\"'{entry.Name}': nested '{def.Name}' was not declared\");\n\t\t\t\t\tAssert.IsTrue(parentIsPlaced || !ModuleClosure.IsPlaced(def.Modules), $\"'{entry.Name}': nests placed '{def.Name}' but has no transform to anchor it\");\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// We only need to validate keyed entries, because only keyed entries can be referenced (and thus be cyclic).\n\t\t\tvar state = new Dictionary<EntityDefKey, byte>(_entryIndexByKey.Count);\n\t\t\tforeach (var kvp in _entryIndexByKey)\n\t\t\t\tAssertNoCyclicalReferences(kvp.Key, state);\n\t\t}\n\n\t\tvoid AssertNoCyclicalReferences(EntityDefKey key, Dictionary<EntityDefKey, byte> state)\n\t\t{\n\t\t\tconst byte Visiting = 1, Done = 2;\n\n\t\t\tvar entry = _allDefinitionEntries[_entryIndexByKey[key]];\n\t\t\tstate.TryGetValue(key, out var mark);\n\t\t\tAssert.IsTrue(mark != Visiting, $\"definition spawn cycle through '{entry.Name}'\");\n// … (truncated)"
     }
    },
    {
     "line": 195,
     "state": {
      "comment": "// A look shared by several definitions is walked once.",
      "annotated_code": "\t\t\tvar walked = new Dictionary<Look, LookSource>();\n\t\t\tvar looks = new LookSource[_allDefinitionEntries.Count];\n\t\t\tfor (int i = 0; i < _allDefinitionEntries.Count; i++)\n\t\t\t\tforeach (var config in _allDefinitionEntries[i].Modules)\n\t\t\t\t{\n\t\t\t\t\tif (config is not LookModuleConfig lookConfig || lookConfig.Look == null)\n\t\t\t\t\t\tcontinue;\n\n\t\t\t\t\tif (!walked.TryGetValue(lookConfig.Look, out var source))\n\t\t\t\t\t\twalked.Add(lookConfig.Look, source = lookConfig.Look.Walk());\n\t\t\t\t\tlooks[i] = source;\n\t\t\t\t}\n\n\t\t\treturn looks;"
     }
    },
    {
     "line": 212,
     "state": {
      "comment": "// Phase 3, after world build. Publishes every keyed entity definition into DefinitionRegistry.",
      "annotated_code": "\t\tpublic void BuildBlobRegistry(DefinitionRegistry definitions)\n\t\t{\n\t\t\tAssertDeclarationsValid();\n\t\t\tdefinitions.CreateDefinitionRegistry(_entryIndexByKey.Count, _timelineCount);\n\t\t\tforeach (var kvp in _entryIndexByKey)\n\t\t\t{\n\t\t\t\tvar entry = _allDefinitionEntries[kvp.Value];\n\t\t\t\tdefinitions.AddEntityDefinition(kvp.Key, kvp.Value, entry.Name, entry.Modules, entry.Nested, entry.Archetype.TagSet);\n\t\t\t}\n\t\t}\n\n\t\tvoid AssertDeclarationsValid()\n\t\t{\n\t\t\tforeach (var entry in _allDefinitionEntries)\n\t\t\t{\n\t\t\t\tbool parentIsPlaced = ModuleClosure.IsPlaced(entry.Modules);\n\t\t\t\tforeach (var n in entry.Nested)\n\t\t\t\t{\n\t\t\t\t\tvar nd = n.DefinitionAssetRef;\n\t\t\t\t\tif (nd == null)\n\t\t\t\t\t\tcontinue;\n\t\t\t\t\tvar def = nd.Definition;\n\t\t\t\t\tAssert.IsTrue(_entryIndexByKey.ContainsKey(def.Id), $\"'{entry.Name}': nested '{def.Name}' was not declared\");\n\t\t\t\t\tAssert.IsTrue(parentIsPlaced || !ModuleClosure.IsPlaced(def.Modules), $\"'{entry.Name}': nests placed '{def.Name}' but has no transform to anchor it\");\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// We only need to validate keyed entries, because only keyed entries can be referenced (and thus be cyclic).\n\t\t\tvar state = new Dictionary<EntityDefKey, byte>(_entryIndexByKey.Count);\n\t\t\tforeach (var kvp in _entryIndexByKey)\n\t\t\t\tAssertNoCyclicalReferences(kvp.Key, state);\n\t\t}\n\n\t\tvoid AssertNoCyclicalReferences(EntityDefKey key, Dictionary<EntityDefKey, byte> state)\n\t\t{\n\t\t\tconst byte Visiting = 1, Done = 2;\n\n\t\t\tvar entry = _allDefinitionEntries[_entryIndexByKey[key]];\n\t\t\tstate.TryGetValue(key, out var mark);\n\t\t\tAssert.IsTrue(mark != Visiting, $\"definition spawn cycle through '{entry.Name}'\");\n\t\t\tif (mark == Done)\n\t\t\t\treturn;\n\n\t\t\tstate[key] = Visiting;\n\t\t\tforeach (var nested in entry.Nested)\n\t\t\t{\n\t\t\t\tvar n = nested.DefinitionAssetRef?.Definition;\n\t\t\t\tif (n != null && _entryIndexByKey.ContainsKey(n.Id))\n\t\t\t\t\tAssertNoCyclicalReferences(n.Id, state);\n\t\t\t}\n\t\t\tforeach (var config in entry.Modules)\n\t\t\t{\n\t\t\t\tif (config is not AbilityOwnerModuleConfig owner)\n\t\t\t\t\tcontinue;\n\t\t\t\tfor (int list = 0; list < 2; list++)\n\t\t\t\t\tforeach (var asset in list == 0 ? owner.Ordered : owner.Auto)\n\t\t\t\t\t{\n\t\t\t\t\t\tvar a = asset?.Definition;\n\t\t\t\t\t\tif (a != null && _entryIndexByKey.ContainsKey(a.Id))\n\t\t\t\t\t\t\tAssertNoCyclicalReferences(a.Id, state);\n// … (truncated)"
     }
    },
    {
     "line": 240,
     "state": {
      "comment": "// We only need to validate keyed entries, because only keyed entries can be referenced (and thus be cyclic).",
      "annotated_code": "\t\t\tvar state = new Dictionary<EntityDefKey, byte>(_entryIndexByKey.Count);\n\t\t\tforeach (var kvp in _entryIndexByKey)\n\t\t\t\tAssertNoCyclicalReferences(kvp.Key, state);"
     }
    }
   ],
   "declarations": [
    {
     "line": 30,
     "head": "static readonly IReadOnlyList<NestedDefinition> NoNested = Array.Empty<NestedDefinition>();",
     "state": {
      "declaration": "\t\tstatic readonly IReadOnlyList<NestedDefinition> NoNested = Array.Empty<NestedDefinition>();",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 32,
     "head": "readonly TemplateRegistry _registry = new();",
     "state": {
      "declaration": "\t\treadonly TemplateRegistry _registry = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 34,
     "head": "readonly List<DefinitionEntry> _allDefinitionEntries = new();",
     "state": {
      "declaration": "\t\treadonly List<DefinitionEntry> _allDefinitionEntries = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 37,
     "head": "readonly Dictionary<EntityDefKey, int> _entryIndexByKey = new();",
     "state": {
      "declaration": "\t\treadonly Dictionary<EntityDefKey, int> _entryIndexByKey = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Published subset.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 40,
     "head": "readonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();",
     "state": {
      "declaration": "\t\treadonly Dictionary<TimelineNodeConfig, EntityDefKey> _nodeKeys = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Dedups timeline nodes.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 42,
     "head": "readonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();",
     "state": {
      "declaration": "\t\treadonly Dictionary<EntityCreateConfig, EntityDefKey> _inlineCreateKeys = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 44,
     "head": "readonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();",
     "state": {
      "declaration": "\t\treadonly Dictionary<EntityDefinitionAsset, EntityDefKey> _assetKeys = new();",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 46,
     "head": "int _generatedCounter;",
     "state": {
      "declaration": "\t\tint _generatedCounter;",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 47,
     "head": "int _timelineCount;",
     "state": {
      "declaration": "\t\tint _timelineCount;",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 50,
     "head": "internal TemplateRegistry.RegisteredArchetype DeclareUnkeyedDefinition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)",
     "state": {
      "declaration": "\t\tinternal TemplateRegistry.RegisteredArchetype DeclareUnkeyedDefinition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)\n\t\t{\n\t\t\tentry = CreateEntry(nameHint, configs, nested);\n\t\t\treturn _allDefinitionEntries[entry].Archetype;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Phase 1, before world build.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 57,
     "head": "public IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();",
     "state": {
      "declaration": "\t\tpublic IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Phase 2.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 59,
     "head": "int DeclareKeyedDefinition(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)",
     "state": {
      "declaration": "\t\tint DeclareKeyedDefinition(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t\t=> _entryIndexByKey[key] = CreateEntry(nameHint, configs, nested);",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 64,
     "head": "int CreateEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)",
     "state": {
      "declaration": "\t\tint CreateEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)\n\t\t{\n\t\t\tnested ??= NoNested;\n\t\t\tModuleClosure.Apply(configs);\n\n\t\t\tusing var pooled = UnityEngine.Pool.ListPool<EntityModule>.Get(out var modules);\n\t\t\tforeach (var config in configs)\n\t\t\t{\n\t\t\t\tAssert.IsNotNull(config, $\"'{nameHint}': empty module config entry\");\n\t\t\t\tswitch (config)\n\t\t\t\t{\n\t\t\t\t\tcase AbilityOwnerModuleConfig owner:\n\t\t\t\t\t\tResolveAbilities(owner);\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase ActionTimelineModuleConfig timeline:\n\t\t\t\t\t\tResolveTimeline(timeline, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t\tcase IEntityCreateModuleConfig create:\n\t\t\t\t\t\tcreate.Create.ResolvedDefinition = InternEntityCreate(create.Create, $\"{nameHint} ▸ {config.Module}\");\n\t\t\t\t\t\tbreak;\n\t\t\t\t}\n\t\t\t\tmodules.Add(config.Module);\n\t\t\t}\n\n\t\t\tvar archetype = _registry.Register(ComposedEntityDefinition.FromModules(nameHint, modules));\n\t\t\tint index = _allDefinitionEntries.Count;\n\t\t\t_allDefinitionEntries.Add(new DefinitionEntry(nameHint, configs, nested, archetype));\n\t\t\tforeach (var n in nested)\n\t\t\t\tif (n.DefinitionAssetRef != null)\n\t\t\t\t\tInternDefinitionAsset(n.DefinitionAssetRef);\n\t\t\treturn index;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Takes ownership of configs and the configs in it: augments the list, modifies the configs.\n// Returns entry index: module walk (before) and nested declare (after) append their own entries.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 97,
     "head": "void ResolveAbilities(AbilityOwnerModuleConfig owner)",
     "state": {
      "declaration": "\t\tvoid ResolveAbilities(AbilityOwnerModuleConfig owner)\n\t\t{\n\t\t\towner.ResolvedOrdered = InternDefinitionAssets(owner.Ordered);\n\t\t\towner.ResolvedAuto = InternDefinitionAssets(owner.Auto);\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 104,
     "head": "EntityDefKey[] InternDefinitionAssets(EntityDefinitionAsset[] assets)",
     "state": {
      "declaration": "\t\tEntityDefKey[] InternDefinitionAssets(EntityDefinitionAsset[] assets)\n\t\t{\n\t\t\tvar keys = new EntityDefKey[assets.Length];\n\t\t\tfor (int i = 0; i < assets.Length; i++)\n\t\t\t\tif (assets[i] != null)\n\t\t\t\t\tkeys[i] = InternDefinitionAsset(assets[i]);\n\t\t\treturn keys;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Null entries keep their slot with a default key.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 113,
     "head": "EntityDefKey InternEntityCreate(EntityCreateConfig config, string context)",
     "state": {
      "declaration": "\t\tEntityDefKey InternEntityCreate(EntityCreateConfig config, string context)\n\t\t{\n\t\t\t// Asset ref: reuses the asset's key.\n\t\t\tif (config.DefinitionAsset != null)\n\t\t\t\treturn InternDefinitionAsset(config.DefinitionAsset);\n\n\t\t\t// Inline: generated key, deduped by config identity.\n\t\t\tif (_inlineCreateKeys.TryGetValue(config, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar def = config.InlineDefinition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\t\t\tif (def.Modules == null || def.Modules.Count == 0)\n\t\t\t\treturn default; // Validate reports it; nothing to declare\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_inlineCreateKeys[config] = key; // set before recursion — guards a definition spawning itself\n\t\t\tDeclareKeyedDefinition(key, context, configs, def.Nested);\n\t\t\treturn key;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 135,
     "head": "internal EntityDefKey InternDefinitionAsset(EntityDefinitionAsset asset)",
     "state": {
      "declaration": "\t\tinternal EntityDefKey InternDefinitionAsset(EntityDefinitionAsset asset)\n\t\t{\n\t\t\tAssert.IsNotNull(asset, \"declare definition: null asset\");\n\t\t\tif (_assetKeys.TryGetValue(asset, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar definition = asset.Definition;\n\t\t\tAssert.IsNotNull(definition, $\"'{asset.name}': empty definition\");\n\t\t\tAssert.IsFalse(definition.Id.IsNone, $\"'{asset.name}': definition without id\");\n\t\t\tAssert.IsFalse(definition.Id.IsGenerated, $\"'{asset.name}': asset id in the compiler-minted range\");\n\t\t\tAssert.IsFalse(_entryIndexByKey.ContainsKey(definition.Id), $\"'{asset.name}': duplicate definition id {definition.Id}\");\n\n\t\t\t_assetKeys[asset] = definition.Id; // set before recursion — guards a definition reaching itself\n\n\t\t\t// Copy: declare augments the list; the asset's serialized modules must stay untouched.\n\t\t\tvar configs = new List<ModuleConfig>(definition.Modules);\n\t\t\tDeclareKeyedDefinition(definition.Id, asset.name, configs, definition.Nested);\n\t\t\treturn definition.Id;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 155,
     "head": "void ResolveTimeline(ActionTimelineModuleConfig timeline, string context)",
     "state": {
      "declaration": "\t\tvoid ResolveTimeline(ActionTimelineModuleConfig timeline, string context)\n\t\t{\n\t\t\t_timelineCount++;\n\t\t\tfor (int i = 0; i < timeline.Nodes.Count; i++)\n\t\t\t{\n\t\t\t\tvar node = timeline.Nodes[i];\n\t\t\t\tAssert.IsNotNull(node, $\"'{context}': empty node entry\");\n\t\t\t\tnode.ResolvedDefinition = InternTimelineNode(node, $\"{context}[{i}]\");\n\t\t\t}\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 166,
     "head": "EntityDefKey InternTimelineNode(TimelineNodeConfig node, string context)",
     "state": {
      "declaration": "\t\tEntityDefKey InternTimelineNode(TimelineNodeConfig node, string context)\n\t\t{\n\t\t\tif (_nodeKeys.TryGetValue(node, out var existing))\n\t\t\t\treturn existing;\n\n\t\t\tvar key = MintGeneratedKey();\n\t\t\t_nodeKeys[node] = key; // before the declare, which recurses back here\n\t\t\tDeclareTimelineNodeDefinition(key, node, context);\n\t\t\treturn key;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 177,
     "head": "void DeclareTimelineNodeDefinition(EntityDefKey key, TimelineNodeConfig node, string context)",
     "state": {
      "declaration": "\t\tvoid DeclareTimelineNodeDefinition(EntityDefKey key, TimelineNodeConfig node, string context)\n\t\t{\n\t\t\tvar def = node.Definition;\n\t\t\tAssert.IsNotNull(def, $\"'{context}': no definition (inline or asset)\");\n\n\t\t\tvar configs = new List<ModuleConfig>(def.Modules);\n\t\t\tModuleClosure.Ensure(configs, EntityModules.ConditionTime, () => new ConditionTimeModuleConfig());\n\n\t\t\tAssert.IsFalse(ModuleClosure.IsPlaced(configs), $\"'{context}': timeline node has a transform, but timeline nodes spawn without a transform anchor\");\n\n\t\t\tDeclareKeyedDefinition(key, node.DefinitionAsset != null ? $\"{context} ▸ {node.DefinitionAsset.name}\" : context, configs, def.Nested);\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 190,
     "head": "EntityDefKey MintGeneratedKey() => EntityDefKey.Generated(_generatedCounter++);",
     "state": {
      "declaration": "\t\tEntityDefKey MintGeneratedKey() => EntityDefKey.Generated(_generatedCounter++);",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 193,
     "head": "public IReadOnlyList<LookSource> BuildLooks()",
     "state": {
      "declaration": "\t\tpublic IReadOnlyList<LookSource> BuildLooks()\n\t\t{\n\t\t\t// A look shared by several definitions is walked once.\n\t\t\tvar walked = new Dictionary<Look, LookSource>();\n\t\t\tvar looks = new LookSource[_allDefinitionEntries.Count];\n\t\t\tfor (int i = 0; i < _allDefinitionEntries.Count; i++)\n\t\t\t\tforeach (var config in _allDefinitionEntries[i].Modules)\n\t\t\t\t{\n\t\t\t\t\tif (config is not LookModuleConfig lookConfig || lookConfig.Look == null)\n\t\t\t\t\t\tcontinue;\n\n\t\t\t\t\tif (!walked.TryGetValue(lookConfig.Look, out var source))\n\t\t\t\t\t\twalked.Add(lookConfig.Look, source = lookConfig.Look.Walk());\n\t\t\t\t\tlooks[i] = source;\n\t\t\t\t}\n\n\t\t\treturn looks;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// By entry, null for none.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 213,
     "head": "public void BuildBlobRegistry(DefinitionRegistry definitions)",
     "state": {
      "declaration": "\t\tpublic void BuildBlobRegistry(DefinitionRegistry definitions)\n\t\t{\n\t\t\tAssertDeclarationsValid();\n\t\t\tdefinitions.CreateDefinitionRegistry(_entryIndexByKey.Count, _timelineCount);\n\t\t\tforeach (var kvp in _entryIndexByKey)\n\t\t\t{\n\t\t\t\tvar entry = _allDefinitionEntries[kvp.Value];\n\t\t\t\tdefinitions.AddEntityDefinition(kvp.Key, kvp.Value, entry.Name, entry.Modules, entry.Nested, entry.Archetype.TagSet);\n\t\t\t}\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "comment": "// Phase 3, after world build. Publishes every keyed entity definition into DefinitionRegistry.",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 224,
     "head": "void AssertDeclarationsValid()",
     "state": {
      "declaration": "\t\tvoid AssertDeclarationsValid()\n\t\t{\n\t\t\tforeach (var entry in _allDefinitionEntries)\n\t\t\t{\n\t\t\t\tbool parentIsPlaced = ModuleClosure.IsPlaced(entry.Modules);\n\t\t\t\tforeach (var n in entry.Nested)\n\t\t\t\t{\n\t\t\t\t\tvar nd = n.DefinitionAssetRef;\n\t\t\t\t\tif (nd == null)\n\t\t\t\t\t\tcontinue;\n\t\t\t\t\tvar def = nd.Definition;\n\t\t\t\t\tAssert.IsTrue(_entryIndexByKey.ContainsKey(def.Id), $\"'{entry.Name}': nested '{def.Name}' was not declared\");\n\t\t\t\t\tAssert.IsTrue(parentIsPlaced || !ModuleClosure.IsPlaced(def.Modules), $\"'{entry.Name}': nests placed '{def.Name}' but has no transform to anchor it\");\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t// We only need to validate keyed entries, because only keyed entries can be referenced (and thus be cyclic).\n\t\t\tvar state = new Dictionary<EntityDefKey, byte>(_entryIndexByKey.Count);\n\t\t\tforeach (var kvp in _entryIndexByKey)\n\t\t\t\tAssertNoCyclicalReferences(kvp.Key, state);\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 246,
     "head": "void AssertNoCyclicalReferences(EntityDefKey key, Dictionary<EntityDefKey, byte> state)",
     "state": {
      "declaration": "\t\tvoid AssertNoCyclicalReferences(EntityDefKey key, Dictionary<EntityDefKey, byte> state)\n\t\t{\n\t\t\tconst byte Visiting = 1, Done = 2;\n\n\t\t\tvar entry = _allDefinitionEntries[_entryIndexByKey[key]];\n\t\t\tstate.TryGetValue(key, out var mark);\n\t\t\tAssert.IsTrue(mark != Visiting, $\"definition spawn cycle through '{entry.Name}'\");\n\t\t\tif (mark == Done)\n\t\t\t\treturn;\n\n\t\t\tstate[key] = Visiting;\n\t\t\tforeach (var nested in entry.Nested)\n\t\t\t{\n\t\t\t\tvar n = nested.DefinitionAssetRef?.Definition;\n\t\t\t\tif (n != null && _entryIndexByKey.ContainsKey(n.Id))\n\t\t\t\t\tAssertNoCyclicalReferences(n.Id, state);\n\t\t\t}\n\t\t\tforeach (var config in entry.Modules)\n\t\t\t{\n\t\t\t\tif (config is not AbilityOwnerModuleConfig owner)\n\t\t\t\t\tcontinue;\n\t\t\t\tfor (int list = 0; list < 2; list++)\n\t\t\t\t\tforeach (var asset in list == 0 ? owner.Ordered : owner.Auto)\n\t\t\t\t\t{\n\t\t\t\t\t\tvar a = asset?.Definition;\n\t\t\t\t\t\tif (a != null && _entryIndexByKey.ContainsKey(a.Id))\n\t\t\t\t\t\t\tAssertNoCyclicalReferences(a.Id, state);\n\t\t\t\t\t}\n\t\t\t}\n\t\t\tstate[key] = Done;\n\t\t}",
      "type_signature": "public sealed class DefinitionCompiler",
      "siblings": [
       "NoNested",
       "_registry",
       "_allDefinitionEntries",
       "_entryIndexByKey",
       "_nodeKeys",
       "_inlineCreateKeys",
       "_assetKeys",
       "_generatedCounter",
       "_timelineCount",
       "DeclareUnkeyedDefinition",
       "BuildTemplates",
       "DeclareKeyedDefinition",
       "CreateEntry",
       "ResolveAbilities",
       "InternDefinitionAssets",
       "InternEntityCreate",
       "InternDefinitionAsset",
       "ResolveTimeline",
       "InternTimelineNode",
       "DeclareTimelineNodeDefinition",
       "MintGeneratedKey",
       "BuildLooks",
       "BuildBlobRegistry",
       "AssertDeclarationsValid",
       "AssertNoCyclicalReferences"
      ]
     }
    },
    {
     "line": 16,
     "head": "public readonly string Name;",
     "state": {
      "declaration": "\t\t\tpublic readonly string Name;",
      "type_signature": "readonly struct DefinitionEntry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "DefinitionEntry"
      ]
     }
    },
    {
     "line": 17,
     "head": "public readonly IReadOnlyList<ModuleConfig> Modules;",
     "state": {
      "declaration": "\t\t\tpublic readonly IReadOnlyList<ModuleConfig> Modules;",
      "type_signature": "readonly struct DefinitionEntry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "DefinitionEntry"
      ]
     }
    },
    {
     "line": 18,
     "head": "public readonly IReadOnlyList<NestedDefinition> Nested;",
     "state": {
      "declaration": "\t\t\tpublic readonly IReadOnlyList<NestedDefinition> Nested;",
      "type_signature": "readonly struct DefinitionEntry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "DefinitionEntry"
      ]
     }
    },
    {
     "line": 19,
     "head": "public readonly TemplateRegistry.RegisteredArchetype Archetype;",
     "state": {
      "declaration": "\t\t\tpublic readonly TemplateRegistry.RegisteredArchetype Archetype;",
      "type_signature": "readonly struct DefinitionEntry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "DefinitionEntry"
      ]
     }
    },
    {
     "line": 21,
     "head": "public DefinitionEntry(string name, IReadOnlyList<ModuleConfig> modules, IReadOnlyList<NestedDefinition> nested, TemplateRegistry.RegisteredArchetype archetype)",
     "state": {
      "declaration": "\t\t\tpublic DefinitionEntry(string name, IReadOnlyList<ModuleConfig> modules, IReadOnlyList<NestedDefinition> nested, TemplateRegistry.RegisteredArchetype archetype)\n\t\t\t{\n\t\t\t\tName = name;\n\t\t\t\tModules = modules;\n\t\t\t\tNested = nested;\n\t\t\t\tArchetype = archetype;\n\t\t\t}",
      "type_signature": "readonly struct DefinitionEntry",
      "siblings": [
       "Name",
       "Modules",
       "Nested",
       "Archetype",
       "DefinitionEntry"
      ]
     }
    }
   ],
   "types": [
    {
     "line": 12,
     "head": "public sealed class DefinitionCompiler",
     "state": {
      "signatures": [
       "internal TemplateRegistry.RegisteredArchetype DeclareUnkeyedDefinition(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested, out int entry)",
       "public IReadOnlyList<Template> BuildTemplates() => _registry.BuildTemplates();",
       "int DeclareKeyedDefinition(EntityDefKey key, string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)",
       "int CreateEntry(string nameHint, List<ModuleConfig> configs, IReadOnlyList<NestedDefinition> nested)",
       "void ResolveAbilities(AbilityOwnerModuleConfig owner)",
       "EntityDefKey[] InternDefinitionAssets(EntityDefinitionAsset[] assets)",
       "EntityDefKey InternEntityCreate(EntityCreateConfig config, string context)",
       "internal EntityDefKey InternDefinitionAsset(EntityDefinitionAsset asset)",
       "void ResolveTimeline(ActionTimelineModuleConfig timeline, string context)",
       "EntityDefKey InternTimelineNode(TimelineNodeConfig node, string context)",
       "void DeclareTimelineNodeDefinition(EntityDefKey key, TimelineNodeConfig node, string context)",
       "EntityDefKey MintGeneratedKey() => EntityDefKey.Generated(_generatedCounter++);",
       "public IReadOnlyList<LookSource> BuildLooks()",
       "public void BuildBlobRegistry(DefinitionRegistry definitions)",
       "void AssertDeclarationsValid()",
       "void AssertNoCyclicalReferences(EntityDefKey key, Dictionary<EntityDefKey, byte> state)"
      ]
     }
    },
    {
     "line": 14,
     "head": "readonly struct DefinitionEntry",
     "state": {
      "signatures": [
       "public DefinitionEntry(string name, IReadOnlyList<ModuleConfig> modules, IReadOnlyList<NestedDefinition> nested, TemplateRegistry.RegisteredArchetype archetype)"
      ]
     }
    }
   ]
  }
 },
 "comparisons": [
  {
   "before": "v0",
   "after": "v1",
   "rules": "comment-rules.json",
   "change": "80 comment lines cut to 23: pointers, history, restatements and prose removed."
  },
  {
   "before": "v1",
   "after": "v2",
   "rules": "rules.json",
   "change": "Declare* split into Intern/Resolve/Create/Assert/Build by what each actually does."
  }
 ]
}
